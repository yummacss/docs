export const meta = {
  authors: ["Renildo"],
  cover: "~",
  date: "2026-01-24",
  description: "We've added support for negative values, new pseudo-class and pseudo-element variants, and expanded utilities. There have also been dependency changes.",
  title: "Yumma CSS 3.9"
};

**This update might change existing features, make sure you check out the [release notes](https://github.com/yummacss/yummacss/releases/tag/v3.9.0) before upgrading.**

---

## Breaking changes

This version introduces some breaking changes that you should be aware of before upgrading.

## API is now called Core

We've renamed the `@yummacss/api` package to `@yummacss/core`. The name "core" better represents the package as the foundation of Yumma CSS, while avoiding potential confusion with other types of APIs.

```bash title="Terminal"
npm remove @yummacss/api
npm install @yummacss/core
```

To avoid naming collisions and improve consistency, we've renamed two utility prefixes:

### Box Shadow

```html del={1} ins={2} title="index.html"
<div class="bs-*">...</div>
<div class="bsh-*">...</div>
```

### Isolation

```html del={1} ins={2} title="index.html"
<div class="i-*">...</div>
<div class="is-*">...</div>
```

---

## New pseudo-variants

This release introduces highly requested pseudo-class and pseudo-element variants that give you even more control over your styling.

### Pseudo-elements

You can now style `::before`, `::after`, `::selection`, and `::placeholder` elements directly using the new variants.

The `s::*` variant lets you customize text selection styling:

<Preview>
  <p className="s::bg-indigo-4 s::c-white c-black">Select this text to see custom selection colors!</p>
</Preview>

```html "s::bg-indigo" "s::c-white"
<p class="s::bg-indigo-4 s::c-white c-black">Select this text to see custom selection colors!</p>
```

And the `p::*` variant styles placeholder text in form inputs:

<Preview>
  <input className="p::c-red-4 p::fs-i bw-1 bc-red-4 br-0 p-3 w-full" placeholder="I said, enter your email!" />
</Preview>

```html "p::c-red-4" "p::fs-i"
<input class="p::c-red-4 p::fs-i bw-1 bc-red-4 br-0 p-3 w-full" placeholder="I said, enter your email!" />
```

### Pseudo-classes

We've added `fv:*` for `:focus-visible` and `fw:*` for `:focus-within`, giving you more precise control over focus states.

<Preview>
  <button className="fv:ow-2 fv:oc-indigo p-px bg-indigo c-white br-0">
    Focus with keyboard
  </button>
</Preview>

```html "fv:ow-2" "fv:oc-indigo"
<button class="fv:ow-2 fv:oc-indigo p-px bg-indigo c-white br-0">
  Focus with keyboard
</button>
```

The `fw:*` variant is perfect for form containers:

<Preview>
  <div className="fw:bc-indigo bw-2 bc-silver p-px br-0">
    <input className="p-2 br-0 bw-1 bc-silver" placeholder="Focus me" />
  </div>
</Preview>

```html "fw:bc-indigo"
<div class="fw:bc-indigo bw-2 bc-silver p-px br-0">
  <input class="p-2 br-0" placeholder="Focus me" />
</div>
```

## Touch device support

The new `pc:*` media query variant targets devices with coarse pointer input (touch screens).

<Preview>
  <button className="p-px pc:p-6 bg-indigo c-white br-0">
    Larger padding on touch devices
  </button>
</Preview>

```html "pc:p-6"
<button class="p-px pc:p-6 bg-indigo c-white br-0">
  Larger padding on touch devices
</button>
```

## Extended cursor utilities

We've added 11 new cursor values to help you create more intuitive interactions:

<UtilityTable category="interactivity" name="cursor" />

## Touch action utilities

Control how touch gestures behave with the new `ta-*` utilities that support all 9 `touch-action` values.

<UtilityTable category="interactivity" name="touch-action" />

## Transform enhancements

### Extended rotation range

The `t-r-*` utility now supports rotation from 0 to 360 degrees in steps of 5, giving you much more flexibility.

<UtilityTable category="transform" name="rotate" />

### Translate utilities

New `t-tx-*` and `t-ty-*` utilities make centering elements easier with `half` and `full` values.

<UtilityTable category="transform" name="translate-x" />

Translate utilities are also available for `y` axis.

<UtilityTable category="transform" name="translate-y" />

## Positioning with auto

You can now use `auto` with `b-*`, `i-*`, `ix-*`, `iy-*`, `l-*`, `r-*`, and `t-*` utilities.

<Preview>
  <div className="p-r">
    <div className="p-auto l-auto r-auto d-14 bg-indigo"></div>
  </div>
</Preview>

```html "l-auto" "r-auto"
<div class="p-r">
  <div class="p-auto l-auto r-auto d-14 bg-indigo"></div>
</div>
```

## Negative value support

One of the most requested features is here! You can now use negative values with the `--` syntax.

<Preview>
  <div className="c-white d-f fs-sm jc-c">
    <div className="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">A</div>
    <div className="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">B</div>
    <div className="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">C</div>
    <div className="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">D</div>
    <div className="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">E</div>
  </div>
</Preview>

```html "ml--2"
<div class="c-white d-f fs-sm jc-c">
  <div class="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">A</div>
  <div class="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">B</div>
  <div class="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">C</div>
  <div class="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">D</div>
  <div class="ml--2 ai-c bg-indigo br-pill d-14 d-f jc-c bw-2 bc-indigo-2/50">E</div>
</div>
```

This works with any utility that accepts numeric values like margins, positioning, and transforms.

## Improved margin and padding

Under the hood, we've updated `mx-*`, `my-*`, `px-*`, and `py-*` utilities to use logical properties (`margin-inline`, `margin-block`, `padding-inline`, `padding-block`). This provides better support for international layouts and different writing modes, with no behavior changes for standard left-to-right layouts.

```css del={1-4, 6-9} ins={11-13, 15-17} title="out.css"
.mx-4 {
  margin-left: 1rem;
  margin-right: 1rem;
}

.py-4 {
  padding-top: 1rem;
  padding-bottom: 1rem;
}

.mx-4 {
  margin-inline: 1rem;
}

.py-4 {
  padding-block: 1rem;
}
```

---

## Upgrade

You can get the latest version of Yumma CSS from npm:

```bash
pnpm install yummacss@latest
```

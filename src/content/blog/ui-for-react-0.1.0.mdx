export const meta = {
  authors: ["Renildo"],
  cover: "~",
  date: "2025-09-18",
  description:
    "We've been hinting at it for a while, but today, we're finally introducing Yumma UI for React! A set of React components built on top of Yumma CSS and Base UI.",
    title: "Yumma UI for React",
};

## What will happen to Yumma UI?

Don't worry, we won't kill our HTML components. It will still be available for you to use without any changes or costs. Think of Yumma UI for React as an expansion pack for your React apps.

---

## Why Yumma UI for React?

Yumma UI for React is a significant step forward for modern React apps. It's a set of accessible and mature React components built on top of Yumma CSS. With its foundation on top of [Base UI](https://base-ui.com).

It's also first component library built on top of Yumma CSS, and we're so happy to finally introduce it to the world. We hope you enjoy using it as much as we do! We're also excited to see what you build with it.

---

## What's in the box?

You can check the components section to learn more about the available components, but anyway, here it is a list of the current components:

```tsx title="index.tsx" showLineNumbers
import {
    Alert,
    Avatar,
    Badge,
    Button,
    Card,
    Checkbox,
    Input,
    Radio,
    Select,
    Tabs
} from "@yummacss/react";
```

### Typescript support

If you use Typescript, you can import the types from `@yummacss/react`. You can also trigger intellisense for the props of each component in any code editor that supports it.

```ts title="index.tsx"
import type { ButtonProps } from "@yummacss/react";
```

### Tree shaking

All components in `@yummacss/react` are tree-shakeable. It's kinda like the same principle as Yumma CSS, meaning you only ship the code you actually use.

```tsx title="index.tsx"
import { Button } from "@yummacss/react";
import { Button, Card, Input } from "@yummacss/react";
```

---

## Getting started

Yumma UI for React is available as a package on [npm](https://www.npmjs.com/package/@yummacss/react). You can install it using your favorite package manager:

<Stepper>
  <Step title="Install Yumma UI for React">
    ```bash
    pnpm add @yummacss/react
    ```
  </Step>

  <Step title="Import styles">
    Import the Yumma UI for React styles in your project.

    ```ts title="main.ts" mark={1}
    import "@yummacss/react/styles.css";
    import "./globals.css";
    ```

  </Step>

  <Step title="Import components">
    That's pretty much it.

    ```tsx title="index.tsx" mark={1,4}
    import { Button } from "@yummacss/react";

    export default function App() {
      return <Button variant="base">Login</Button>;
    }
    ```

  </Step>
</Stepper>

---

## What's next?

This 0.1.0 release is just the beginning. It's very likely that we'll add 10 more components for each minor release, but we're also open to community feedback. So, if you have any ideas, please [open an issue](https://github.com/yumma-lib/yumma-ui/issues) or [send us a tweet](https://x.com/yummacss).

- **More components**: Combobox, Toast, Tooltip, Menubar, and much more
- **Theming component**: Customize colors, typography, radii, and other design choices
- **Framework support**: Support for other frameworks like Astro
- And much more...

## Closing thoughts

We can't wait to see what you build with Yumma UI for React! Remember, this is our first release and is currently only meant for React frameworks, but we're working on support for others.

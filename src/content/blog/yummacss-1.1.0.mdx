---
authors:
- Renildo
cover: ~
date: 2024-01-24
description: "This update isn't going to introduce any major new features. It's more about syntax changes. Anyway, here it is - Yumma CSS v1.1.0"
title: Yumma CSS 1.1
---

## Button Transition

Refreshed the `btn-(value)` component with transitions for a more polished feel.

---

## Font Sizes

Added the `fs-xs` utility for smaller typography controls.

| Utility | Property             |
| ------- | -------------------- |
| `fs-xs` | `font-size: 0.6rem;` |

---

## Dimension Utilities

Introduced the `dim-(value)` utility to replace **height** & **width** utilities for more concise layouts.

```html del={1} ins={2}
<div class="w-12 h-12"></div>
<div class="dim-12"></div>
```

---

## Media Query Changes

Renamed the `xsm` & `xlg` breakpoints to `xs` & `xl` for consistency.

```scss del={1,2} ins={3,4}
"xsm": 0,
"xlg": 1200px
"xs": 0,
"xl": 1200px
```

---

## Utility Changes

Refined utility syntax & introduced minor increments across several utilities.

### Box Shadow

```html del={1,2} ins={3,4}
<div class="bs-xsm"></div>
<div class="bs-xlg"></div>
<div class="bs-xs"></div>
<div class="bs-xl"></div>
```

### Font Sizes

```html del={1} ins={2}
<div class="fs-xlg"></div>
<div class="fs-xl"></div>
```

### Columns

```html del={1} ins={2}
<div class="col-2"></div>
<div class="cols-2"></div>
```


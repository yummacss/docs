export const meta = {
  title: "Border Top Left Radius",
  description: "Set the top-left border radius for an element.",
};

<Baseline path="css.properties.border-top-left-radius" />

<Reference category="border" name="border-top-left-radius" />

## Usage

Apply a `0.5 rem` radius to the top-left corner of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo btlr-2"></div>
</Preview>

```html "btlr-2"
<div class="w-16 h-16 bg-indigo btlr-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="btlr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="btlr" />

## Logical Properties

Use logical properties to adapt to the document's writing mode & direction.

### Border Start Start Radius

Set the logical start-start border radius for an element.

<Baseline path="css.properties.border-start-start-radius" />

<Reference category="border" name="border-start-start-radius" />

<Preview>
  <div className="w-16 h-16 bg-indigo bssr-2"></div>
</Preview>

```html "bssr-2"
<div class="w-16 h-16 bg-indigo bssr-2"></div>
```

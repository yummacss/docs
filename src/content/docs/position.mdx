export const meta = {
  title: "Position",
  description: "Set the positioning of an element.",
};

<Baseline path="css.properties.position" />

<Reference category="positioning" name="position" />

## Usage

### Static

Position the element according to the normal document flow.

<Preview variant="inline">
  <div className="c-white">
    <div className="p-s p-4 bg-indigo">Static Position</div>
    <div className="p-4 bg-indigo-2 c-slate">Another Element</div>
  </div>
</Preview>

```html "p-s"
<div class="c-white">
  <div class="p-s p-4 bg-indigo">Static Position</div>
  <div class="p-4 bg-indigo-2 c-slate">Another Element</div>
</div>
```

### Absolute

Position the element relative to its nearest positioned ancestor.

<Preview variant="inline">
  <div className="p-r h-32 p-4 bg-indigo-2 c-white">
    <div className="l-4 t-4 p-a p-4 bg-indigo">Absolute Position</div>
  </div>
</Preview>

```html "p-a"
<div class="p-r h-32 p-4 bg-indigo-2 c-white">
  <div class="l-4 t-4 p-a p-4 bg-indigo">Absolute Position</div>
</div>
```

### Fixed

Position the element relative to the viewport.

```html "p-f"
<div class="c-white">
  <div class="r-4 t-4 p-f p-4 bg-indigo">Fixed Position</div>
  <div class="h-s p-4 bg-indigo-2 c-white">Scroll down to see the fixed element.</div>
</div>
```

### Relative

Position the element relative to its normal position.

<Preview variant="inline">
  <div className="c-white">
    <div className="l-4 t-4 p-r p-4 bg-indigo">Relative Position</div>
    <div className="p-4 bg-indigo-2 c-slate">Another Element</div>
  </div>
</Preview>

```html "p-r"
<div class="c-white">
  <div class="l-4 t-4 p-r p-4 bg-indigo">Relative Position</div>
  <div class="p-4 bg-indigo-2 c-slate">Another Element</div>
</div>
```

### Sticky

<Hint icon="mouse_scroll">Try scrolling through the container to see how the sticky behavior works.</Hint>

Stick the element to a specified threshold within its container.

<Preview variant="inline">
  <div className="b-1 o-y-s h-64 c-white">
    <div className="t-0 p-st p-4 bg-indigo">Sticky Position</div>
    <div className="h-96 p-4 bg-indigo-2 c-slate">Scroll down to see the sticky behavior.</div>
  </div>
</Preview>

```html "p-st"
<div class="b-1 o-y-s h-64 c-white">
  <div class="t-0 p-st p-4 bg-indigo">Sticky Position</div>
  <div class="h-96 p-4 bg-indigo-2 c-slate">Scroll down to see the sticky behavior.</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="p" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="p" />

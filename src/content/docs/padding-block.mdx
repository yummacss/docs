export const meta = {
  title: "Padding Block",
  description: "Control an element's vertical padding.",
};

<Baseline path="css.properties.padding-block" />

<Reference category="boxModel" name="padding-block" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="py" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="py" />

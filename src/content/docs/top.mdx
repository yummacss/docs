export const meta = {
  title: "Top",
  description: "Set the top placement of positioned elements.",
};

<Baseline path="css.properties.top" />

<Reference category="positioning" name="top" />

## Usage

```html "t-0"
<div class="p-r w-18 h-18 sm:w-32 sm:h-32 c-white">
  <div class="p-a i-0 b-1"></div>
  <div class="p-a t-0 r-0 d-f w-12 h-12 ai-c jc-c bg-indigo p-4">A</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="t" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="t" />

### Negative Values

Use negative variants to position in the opposite direction.

<NegativeValues prefix="t" />

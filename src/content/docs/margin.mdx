export const meta = {
  title: "Margin",
  description: "Set the margin for an element.",
};

<Baseline path="css.properties.margin" />

## Usage

<Reference category="boxModel" name="margin" />

<Preview>
  <div className="d-f fd-r">
    <div className="d-f ai-c jc-c w-16 h-16 m-8 bg-indigo c-white">8</div>
  </div>
</Preview>

```html "m-8"
<div class="d-f fd-r">
  <div class="d-f ai-c jc-c w-16 h-16 m-8 bg-indigo c-white">8</div>
</div>
```


### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="m" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="m" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="m" />

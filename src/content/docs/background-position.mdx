export const meta = {
  title: "Background Position",
  description: "Set the background image position within an element.",
};

<Baseline path="css.properties.background-position" />

<Reference category="background" name="background-position" />

## Usage

```html
<div class="bp-lt ..."></div>
<div class="bp-t ..."></div>
<div class="bp-rt ..."></div>
<div class="bp-l ..."></div>
<div class="bp-c ..."></div>
<div class="bp-r ..."></div>
<div class="bp-lb ..."></div>
<div class="bp-b ..."></div>
<div class="bp-rb ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bp" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bp" />

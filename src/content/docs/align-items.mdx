export const meta = {
  title: "Align Items",
  description: "Set item alignment along a container's cross axis.",
};

<Baseline path="css.properties.align-items" />

<Reference category="flexbox" name="align-items" />

## Usage

```html
<div class="ai-b ..."></div>
<div class="ai-c ..."></div>
<div class="ai-fe ..."></div>
<div class="ai-fs ..."></div>
<div class="ai-s ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="ai" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="ai" />

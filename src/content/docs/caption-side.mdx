export const meta = {
  title: "Caption Side",
  description: "Set the vertical position of a table caption.",
};

<Baseline path="css.properties.caption-side" />

<Reference category="table" name="caption-side" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="table" name="caption-side" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="table" name="caption-side" />

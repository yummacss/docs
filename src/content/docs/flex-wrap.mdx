export const meta = {
  title: "Flex Wrap",
  description: "Set how flex items wrap in a container.",
};

<Baseline path="css.properties.flex-wrap" />

<Reference category="flexbox" name="flex-wrap" />

## Usage

### No Wrap

Prevent items from wrapping to the next line.

<Preview>
  <div className="p-r c-white">
    <div className="d-f fw-nw g-4 max-w-30">
      <div className="d-f ai-c jc-c p-4 bg-indigo">A</div>
      <div className="d-f ai-c jc-c p-4 bg-indigo">B</div>
      <div className="d-f ai-c jc-c p-4 bg-indigo">C</div>
    </div>
  </div>
</Preview>

```html "fw-nw"
<div class="p-r c-white">
  <div class="d-f fw-nw g-4 max-w-30">
    <div class="d-f ai-c jc-c p-4 bg-indigo">A</div>
    <div class="d-f ai-c jc-c p-4 bg-indigo">B</div>
    <div class="d-f ai-c jc-c p-4 bg-indigo">C</div>
  </div>
</div>
```

### Wrap

Allow items to wrap onto multiple lines.

<Preview>
  <div className="p-r c-white">
    <div className="d-f fw-w g-4 max-w-30">
      <div className="d-f ai-c jc-c p-4 bg-indigo">A</div>
      <div className="d-f ai-c jc-c p-4 bg-indigo">B</div>
      <div className="d-f ai-c jc-c p-4 bg-indigo">C</div>
    </div>
  </div>
</Preview>

```html "fw-w"
<div class="p-r c-white">
  <div class="d-f fw-w g-4 max-w-30">
    <div class="d-f ai-c jc-c p-4 bg-indigo">A</div>
    <div class="d-f ai-c jc-c p-4 bg-indigo">B</div>
    <div class="d-f ai-c jc-c p-4 bg-indigo">C</div>
  </div>
</div>
```

### Wrap Reverse

Allow items to wrap onto multiple lines in reverse order.

<Preview>
  <div className="p-r c-white">
    <div className="d-f fw-wr g-4 max-w-30">
      <div className="d-f ai-c jc-c p-4 bg-indigo">A</div>
      <div className="d-f ai-c jc-c p-4 bg-indigo">B</div>
      <div className="d-f ai-c jc-c p-4 bg-indigo">C</div>
    </div>
  </div>
</Preview>

```html "fw-wr"
<div class="p-r c-white">
  <div class="d-f fw-wr g-4 max-w-30">
    <div class="d-f ai-c jc-c p-4 bg-indigo">A</div>
    <div class="d-f ai-c jc-c p-4 bg-indigo">B</div>
    <div class="d-f ai-c jc-c p-4 bg-indigo">C</div>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="fw" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="fw" />

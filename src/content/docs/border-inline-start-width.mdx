export const meta = {
  title: "Border Inline Start Width",
  description: "Set the logical inline-start border width for an element.",
};

<Baseline path="css.properties.border-inline-start-width" />

<Reference category="border" name="border-inline-start-width" />

## Usage

Apply a `0.25 rem` logical inline-start border width to an element.

<Preview>
  <div className="w-16 h-16 bc-indigo bs-s bisw-4"></div>
</Preview>

```html "bisw-4"
<div class="w-16 h-16 bc-indigo bs-s bisw-4"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bisw" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bisw" />

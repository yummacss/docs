export const meta = {
  title: "Tab Size",
  description: "Set the width of horizontal tab characters.",
};

<Baseline path="css.properties.tab-size" />

<Reference category="text" name="tab-size" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="text" name="tab-size" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="text" name="tab-size" />

### Negative Values

Use negative variants to apply negative tab size values.

<NegativeValues category="text" name="tab-size" />

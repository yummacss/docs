export const meta = {
  title: "Field Sizing",
  description: "Controls the sizing of elements that have a default preferred size.",
};

<ApiTable category="interactivity" name="field-sizing" />

## Fixed

> Initial value

Allows the field to size itself based on the content, adjusting its size dynamically to fit the content inside.

<Preview className="bg-white">
  <div className="d-f fd-c">
    <label className="mb-2 tc-black">Write a message:</label>
    <textarea className="b-1 bc-gray fs-f max-h-50 max-w-100 r-none"></textarea>
  </div>
</Preview>

```html "fs-f"
<div class="d-f fd-c">
  <label class="mb-2 tc-black">Write a message:</label>
  <textarea class="b-1 bc-gray fs-f max-h-50 max-w-100 r-none"></textarea>
</div>
```

## Content

Ensures that the field maintains a fixed size, regardless of the content within it.

<Preview className="bg-white">
  <div className="d-f fd-c">
    <label className="mb-2 tc-black">Write a message:</label>
    <textarea className="b-1 bc-gray fs-c max-h-50 max-w-100 r-none"></textarea>
  </div>
</Preview>

```html "fs-c"
<div class="d-f fd-c">
  <label class="mb-2 tc-black">Write a message:</label>
  <textarea class="b-1 bc-gray fs-c max-h-50 max-w-100 r-none"></textarea>
</div>
```

## Using utility variants

Learn how to override existing utilities based on the user's [screen size](/docs/media-queries) or other factors, such as [hover states](/docs/pseudo-classes).

### Using media queries

<MediaVariant class="fs-f" variant="fs-c" classPrefix="fs" />

### Using hover states

<HoverVariant class="fs-f" variant="fs-c" classPrefix="fs" />

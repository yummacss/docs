export const meta = {
  title: "Field Sizing",
  description: "Set how form fields size themselves based on content.",
};

<Baseline path="css.properties.field-sizing" />

<Reference category="interactivity" name="field-sizing" />

## Usage

```html
<textarea class="fs-f ..."></textarea>
<textarea class="fs-c ..."></textarea>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="fs" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="fs" />

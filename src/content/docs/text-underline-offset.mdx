export const meta = {
  title: "Text Underline Offset",
  description: "Set the offset of a text underline.",
};

<Baseline path="css.properties.text-underline-offset" />

<Reference category="text" name="text-underline-offset" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="text" name="text-underline-offset" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="text" name="text-underline-offset" />

### Negative Values

Use negative variants to apply negative underline offset.

<NegativeValues category="text" name="text-underline-offset" />

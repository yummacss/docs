export const meta = {
  title: "Overflow Y",
  description: "Set how content overflows on the vertical axis.",
};

<Baseline path="css.properties.overflow-y" />

<Reference category="positioning" name="overflow-y" />

## Usage

```html
<div class="o-y-v ...">Visible</div>
<div class="o-y-auto ...">Auto</div>
<div class="o-y-c ...">Clip</div>
<div class="o-y-h ...">Hidden</div>
<div class="o-y-s ...">Scroll</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="o-y" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="o-y" />

export const meta = {
  title: "Resize",
  description: "Controls the way an item can be resized.",
};

<Baseline path="css.properties.resize" />

<ApiTable category="interactivity" name="resize" />

## None

> Initial value

Prevents the element from being resized by the user.

<Note icon="size">Try resizing the text area to see how it changes in size.</Note>

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 tc-slate">Write a message:</label>
    <textarea className="b-1 r-none b-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-none"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="b-1 r-none b-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

## Both

Allows the element to be resized both horizontally and vertically.

<Note icon="size">Try resizing the text area to see how it changes in size.</Note>

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 tc-slate">Write a message:</label>
    <textarea className="b-1 max-h-50 max-w-100 r-b b-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-b"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="b-1 max-h-50 max-w-100 r-b b-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

## Horizontal

Allows the element to be resized only in the horizontal direction.

<Note icon="size">Try resizing the text area to see how it changes in size.</Note>

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 tc-slate">Write a message:</label>
    <textarea className="b-1 max-w-100 r-h b-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-h"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="b-1 max-w-100 r-h b-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

## Vertical

Allows the element to be resized only in the vertical direction.

<Note icon="size">Try resizing the text area to see how it changes in size.</Note>

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 tc-slate">Write a message:</label>
    <textarea className="b-1 max-h-50 r-v" rows={4}></textarea>
  </div>
</Preview>

```html "r-v"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="b-1 max-h-50 r-v" rows="4"></textarea>
</div>
```

## Using utility variants

Learn how to override existing utilities based on the user's [screen size](/docs/media-queries) or other factors, such as [hover states](/docs/pseudo-classes).

### Targeting different viewports

<MediaVariant class="r-none" variant="r-h" classPrefix="r" />

### Targeting hover states

<HoverVariant class="r-none" variant="r-h" classPrefix="r" />

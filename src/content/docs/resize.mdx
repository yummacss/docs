export const meta = {
  title: "Resize",
  description: "Control how an element can be resized.",
};

<Baseline path="css.properties.resize" />

<Reference category="interactivity" name="resize" />

## Usage

### None

Prevent an element from being resized.

<Hint icon="size">Try resizing the text area to see how it changes in size.</Hint>

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 c-slate">Write a message:</label>
    <textarea className="bw-1 r-none bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-none"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="bw-1 r-none bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

### Both

Allow an element to be resized both horizontally and vertically.

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 c-slate">Write a message:</label>
    <textarea className="bw-1 max-h-50 max-w-100 r-b bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-b"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="bw-1 max-h-50 max-w-100 r-b bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

### Horizontal

Allow an element to be resized horizontally.

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 c-slate">Write a message:</label>
    <textarea className="bw-1 max-w-100 r-h bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-h"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="bw-1 max-w-100 r-h bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

### Vertical

Allow an element to be resized vertically.

<Preview>
  <div className="d-f fd-c">
    <label className="mb-2 c-slate">Write a message:</label>
    <textarea className="bw-1 max-h-100 r-v bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows={4}></textarea>
  </div>
</Preview>

```html "r-v"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="bw-1 max-h-100 r-v bw-1 bc-silver-4 px-3 py-2 fs-md f:oc-silver-1 f:os-s f:ow-2" rows="4"></textarea>
</div>
```

### Responsive Design

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="r" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="r" />

export const meta = {
  title: "Max Height",
  description: "Limit the maximum height of an element.",
};

<Baseline path="css.properties.max-height" />

<Reference category="boxModel" name="max-height" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="max-height" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="max-height" />

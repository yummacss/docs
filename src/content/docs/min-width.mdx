export const meta = {
  title: "Min Width",
  description: "Set the minimum width of an element.",
};

<Baseline path="css.properties.min-width" />

<Reference category="boxModel" name="min-width" />

<Hint icon="arrows_horizontal">Try changing the horizontal size of the box.</Hint>

<Preview>
  <div className="d-f o-auto ai-c jc-c max-w-64 min-w-16 p-6 w-32 bg-indigo c-white r-h">32</div>
</Preview>

```html "min-w-16"
<div class="d-f o-auto ai-c jc-c max-w-64 min-w-16 p-6 w-32 bg-indigo c-white r-h">32</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="min-w" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="min-h" />

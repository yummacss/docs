export const meta = {
  title: "Word Break",
  description:
    "Specify how words should break when reaching the edge of a line.",
};

<Baseline path="css.properties.word-break" />

<Reference category="text" name="word-break" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes.

<ResponsiveVariant category="text" name="word-break" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="text" name="word-break" />

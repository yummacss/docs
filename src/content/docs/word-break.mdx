export const meta = {
  title: "Word Break",
  description: "Specify how words should break when reaching the edge of a line.",
};

<Baseline path="css.properties.word-break" />

<Reference category="text" name="word-break" />

## Usage

## Usage

```html
<p class="wb-n ...">Normal</p>
<p class="wb-ba ...">Break All</p>
<p class="wb-ka ...">Keep All</p>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes.

<ResponsiveVariant prefix="wb" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="wb" />

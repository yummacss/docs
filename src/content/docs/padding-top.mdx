export const meta = {
  title: "Padding Top",
  description: "Set an element's top padding.",
};

<Baseline path="css.properties.padding-top" />

<Reference category="boxModel" name="padding-top" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="padding-top" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="padding-top" />

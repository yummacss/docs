export const meta = {
  title: "Border Width",
  description: "Set the width for element borders.",
};

<Baseline path="css.properties.border-width" />

<Reference category="border" name="border-width" />

## Usage

```html
<div class="bw-1 ..."></div>
<div class="bw-2 ..."></div>
<div class="bw-3 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bw" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bw" />

### Negative Values

Use negative variants to apply negative border width values.

<NegativeValues prefix="bw" />

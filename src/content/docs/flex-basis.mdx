export const meta = {
  title: "Flex Basis",
  description: "Set the initial size for flex items.",
};

<Baseline path="css.properties.flex-basis" />

<Reference category="flexbox" name="flex-basis" />

## Usage

<Preview variant="inline">
  <div className="c-white">
    <div className="d-f g-4 h-24 w-full">
      <div className="d-f ai-c fb-0 jc-c p-4 bg-indigo">A</div>
      <div className="d-f ai-c fb-50 jc-c p-4 bg-indigo">B</div>
      <div className="d-f ai-c fb-full jc-c p-4 bg-indigo">C</div>
    </div>
  </div>
</Preview>

```html "fb-0" "fb-50" "fb-auto" "fb-full"
<div class="c-white">
  <div class="d-f g-4 h-24 w-full">
    <div class="d-f ai-c fb-0 jc-c p-4 bg-indigo">A</div>
    <div class="d-f ai-c fb-50 jc-c p-4 bg-indigo">B</div>
    <div class="d-f ai-c fb-full jc-c p-4 bg-indigo">C</div>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="fb" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="fb" />

### Negative Values

Use negative variants to apply negative flex basis values.

<NegativeValues prefix="fb" />

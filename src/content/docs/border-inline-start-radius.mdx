export const meta = {
  title: "Border Inline Start Radius",
  description: "Set the logical inline-start border radius for an element.",
};

<Baseline path="css.properties.border-inline-start-radius" />

<Reference category="border" name="border-inline-start-radius" />

## Usage

Apply a `0.5 rem` radius to the logical inline-start border of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo bisr-2"></div>
</Preview>

```html "bisr-2"
<div class="w-16 h-16 bg-indigo bisr-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bisr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bisr" />

---
title: Padding
banner:
  content: Support Yumma CSS by <a href=https://www.yummacss.com/docs/next>starring us on GitHub</a>!
description: Controls the padding of an element.
slug: docs/padding
---

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="p-" incrementValue={0.25} propNames={["padding"]} range={100} unit="rem" />

This example sets the padding to **2rem**. The element will have uniform padding inside it.

```html live "p-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white p-6">6</p>
  </div>
</div>
```

## Padding Bottom

Controls the bottom padding of an element.

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="pb-" incrementValue={0.25} propNames={["padding-bottom"]} range={100} unit="rem" />

This example sets the bottom padding to **2rem**. The element will have space inside it at the bottom.

```html live "pb-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white pb-6">6</p>
  </div>
</div>
```

## Padding Left

Controls the left padding of an element.

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="pl-" incrementValue={0.25} propNames={["padding-left"]} range={100} unit="rem" />

This example sets the left padding to **2rem**. The element will have space inside it on the left side.

```html live "pl-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white pl-6">6</p>
  </div>
</div>
```

## Padding Right

Controls the right padding of an element.

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="pr-" incrementValue={0.25} propNames={["padding-right"]} range={100} unit="rem" />

This example sets the right padding to **2rem**. The element will have space inside it on the right side.

```html live "pr-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white pr-6">6</p>
  </div>
</div>
```

## Padding Top

Controls the top padding of an element.

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="pt-" incrementValue={0.25} propNames={["padding-top"]} range={100} unit="rem" />

This example sets the top padding to **2rem**. The element will have space inside it at the top.

```html live "pt-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white pt-6">6</p>
  </div>
</div>
```

## Padding X

Controls the left and right paddings of an element at once.

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="px-" incrementValue={0.25} propNames={["padding-left", "padding-right"]} range={100} unit="rem" />

This example sets the left and right padding to **2rem**. The element will have uniform space on both sides inside it.

```html live "px-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white px-6">6</p>
  </div>
</div>
```

## Padding Y

Controls the bottom and top paddings of an element at once.

<Utility additionalClasses={[{ name: "auto", value: "auto" }]} classPrefix="py-" incrementValue={0.25} propNames={["padding-bottom", "padding-top"]} range={100} unit="rem" />

This example sets the bottom and top to **2rem**. The element will have uniform space above and below its content.

```html live "py-6" "p-8"
<div class="stripes">
  <div class="p-8">
    <p class="bg-indigo tc-white py-6">6</p>
  </div>
</div>
```

## Conditional styles

<Modifier
  utility="p-1"
  prefixUtil="p"
  modifiers={{
    hoverMod: "p-2",
    responsiveMod: "p-2",
  }}
/>

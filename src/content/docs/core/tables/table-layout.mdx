---
title: Table Layout
banner:
  content: |
    Get ready for the <a href="next">next version of Yumma CSS</a>, which is going to be amazing!
description: Controls the table cell layout algorithm
slug: docs/table-layout
---

Controls the table cell layout algorithm.

<Utility
  data={[
    {
      className: "tl-auto",
      properties: ["table-layout: auto;"],
    },
    {
      className: "tl-f",
      properties: ["table-layout: fixed;"],
    },
  ]}
/>

## Auto

This example sets the table layout to **auto**. The browser will determine the column widths based on the content.

```html live "tl-auto" layout="/src/layouts/wide.astro"
<p class="fw-600 ta-c">We're still working on this one!</p>
```

## Fixed

This example sets the table layout to **fixed**. The column widths are set by the width of the table and the first row.

```html live "tl-f" layout="/src/layouts/wide.astro"
<p class="fw-600 ta-c">We're still working on this one!</p>
```

---

<Modifier
  utility="tl-f"
  prefixUtil="tl"
  modifiers={{
    hoverUtil: "tl-a",
    responsiveUtil: "tl-a",
  }}
/>

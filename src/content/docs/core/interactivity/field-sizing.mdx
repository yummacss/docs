---
title: Field Sizing
banner:
  content: Sunsetting  <a href=https://www.yummacss.com/blog/sunsetting-intellisense>Yumma CSS Intellisense</a> extension 👋
description: Controls the sizing of elements that have a default preferred size.
slug: docs/field-sizing
draft: true
---

<Class name="field-sizing" />

## Content

This example sets the field sizing to **fixed**. The **fs-f** utility ensures that the field maintains a fixed size, regardless of the content within it.

```html live "fs-c"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="b-1 b-s fs-c max-h-50 max-w-100 r-none rad-1"></textarea>
</div>
```

## Fixed

This example sets the field sizing to **content**. The **fs-c** utility allows the field to size itself based on the content, adjusting its size dynamically to fit the content inside.

```html live "fs-f"
<div class="d-f fd-c">
  <label class="mb-2">Write a message:</label>
  <textarea class="b-1 b-s fs-f max-h-50 max-w-100 r-none rad-1"></textarea>
</div>
```

## Conditional styles

<Modifier
  utility="fs-f"
  prefixUtil="fs"
  modifiers={{
    hoverMod: "fs-c",
    responsiveMod: "fs-c",
  }}
/>

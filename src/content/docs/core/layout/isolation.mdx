---
title: "Isolation"
banner:
  content: |
    Get ready for the <a href="https://www.yummacss.com/docs/next">next version of Yumma CSS</a>, which is going to be amazing!
description: "Controls whether an element must create a new stacking context."
slug: "docs/isolation"
draft: true
---

Controls whether an element must create a new stacking context.

<Class name="/isolation" />

## Auto

This example sets the isolation to **auto**. The element will use the default isolation behavior, allowing overlapping elements to blend together.

```html live "i-auto" layout="/src/layouts/wide.astro"
<p class="fw-600 ta-c">We are currently working on this example.</p>
```

## Isolate

This example sets the isolation to **isolate**. The element will create a new stacking context, preventing overlapping elements from blending with the background.

```html live "i-i" layout="/src/layouts/wide.astro"
<p class="fw-600 ta-c">We are currently working on this example.</p>
```

<Modifier
  utility="i-auto"
  prefixUtil="i"
  modifiers={{
    hoverUtil: "i-i",
    responsiveUtil: "i-i",
  }}
/>

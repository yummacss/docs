export const meta = {
  title: "Max Inline Size",
  description: "Control an element's maximum logical inline size.",
};

<Baseline path="css.properties.max-inline-size" />

<Reference category="boxModel" name="max-inline-size" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="max-inline-size" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="max-inline-size" />

### Negative Values

Use negative variants to apply negative dimensions.

<NegativeValues category="boxModel" name="max-inline-size" />

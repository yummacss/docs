export const meta = {
  title: "Text Orientation",
  description:
    "Set the orientation of text characters.",
};

<Baseline path="css.properties.text-orientation" />

<Reference category="text" name="text-orientation" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="text" name="text-orientation" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="text" name="text-orientation" />
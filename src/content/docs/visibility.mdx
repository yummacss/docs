export const meta = {
  title: "Visibility",
  description: "Set whether an element is visible or hidden.",
};

<Baseline path="css.properties.visibility" />

<Reference category="positioning" name="visibility" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="positioning" name="visibility" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="positioning" name="visibility" />

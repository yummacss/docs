export const meta = {
  title: "Grid Row",
  description: "Set the size & position of items across grid rows.",
};

## Usage

<Reference category="grid" name="grid-row" />

```html
<div class="gr-s-2 ..."></div>
<div class="gre-3 ..."></div>
<div class="grs-2 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="grid" name="grid-row" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="grid" name="grid-row" />

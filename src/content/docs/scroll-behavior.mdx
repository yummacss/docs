export const meta = {
  title: "Scroll Behavior",
  description: "Set the scroll behavior for internal navigation.",
};

<Baseline path="css.properties.scroll-behavior" />

<Reference category="interactivity" name="scroll-behavior" />

## Usage

```html
<div class="sb-auto ..."></div>
<div class="sb-s ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="sb" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="sb" />

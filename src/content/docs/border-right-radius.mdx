export const meta = {
  title: "Border Right Radius",
  description: "Set the right border radius for an element.",
};

<Baseline path="css.properties.border-right-radius" />

<Reference category="border" name="border-right-radius" />

## Usage

Apply a `0.5 rem` radius to the right border of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo brr-2"></div>
</Preview>

```html "brr-2"
<div class="w-16 h-16 bg-indigo brr-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="brr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="brr" />

## Logical Properties

Use logical properties to adapt to the document's writing mode & direction.

### Border Inline End Radius

Set the logical inline-end border radius for an element.

<Baseline path="css.properties.border-inline-end-radius" />

<Reference category="border" name="border-inline-end-radius" />

<Preview>
  <div className="w-16 h-16 bg-indigo bier-2"></div>
</Preview>

```html "bier-2"
<div class="w-16 h-16 bg-indigo bier-2"></div>
```

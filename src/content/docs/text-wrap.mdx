## Usage

```html
<p class="tw-w ...">Wrap</p>
<p class="tw-b ...">Balance</p>
<p class="tw-nw ...">No Wrap</p>
<p class="tw-p ...">Pretty</p>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="text" name="text-wrap" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="text" name="text-wrap" />

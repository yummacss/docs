export const meta = {
  title: "Pointer Events",
  description: "Set how an item responds to pointer events.",
};

<Baseline path="css.properties.pointer-events" />

<Reference category="interactivity" name="pointer-events" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="interactivity" name="pointer-events" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="interactivity" name="pointer-events" />

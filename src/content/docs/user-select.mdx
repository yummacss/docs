export const meta = {
  title: "User Select",
  description: "Set the ability for users to select text in an element.",
};

<Baseline path="css.properties.user-select" />

<Reference category="interactivity" name="user-select" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="interactivity" name="user-select" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="interactivity" name="user-select" />

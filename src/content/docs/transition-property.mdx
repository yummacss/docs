export const meta = {
  title: "Transition Property",
  description: "Set which properties have a transition effect.",
};

<Baseline path="css.properties.transition-property" />

<Reference category="transition" name="transition-property" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="transition" name="transition-property" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="transition" name="transition-property" />

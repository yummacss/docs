export const meta = {
  title: "Padding Inline",
  description: "Control an element's horizontal padding.",
};

<Baseline path="css.properties.padding-inline" />

<Reference category="boxModel" name="padding-inline" />

## Usage

<Preview>
  <div>
    <div className="p-8">
      <p className="px-6 bg-indigo c-white">6</p>
    </div>
  </div>
</Preview>

```html "px-6"
<div>
  <div class="p-8">
    <p class="px-6 bg-indigo c-white">6</p>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="px" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="px" />

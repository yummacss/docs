export const meta = {
  title: "Grid Column End",
  description: "Control where a grid item ends within the grid columns.",
};

<Baseline path="css.properties.grid-column-end" />

<Reference category="grid" name="grid-column-end" />

## Usage

<Preview variant="inline">
    <div className="d-g g-4 gtc-4 c-white ta-c">
      <div className="gce-3 p-4 bg-indigo">A</div>
      <div className="p-4 bg-indigo">B</div>
      <div className="p-4 bg-indigo">C</div>
    </div>
</Preview>

```html "gce-3"
<div class="d-g g-4 gtc-4 c-white ta-c">
  <div class="gce-3 p-4 bg-indigo">A</div>
  <div class="p-4 bg-indigo">B</div>
  <div class="p-4 bg-indigo">C</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="gce" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="gce" />

export const meta = {
  title: "Overflow X",
  description: "Set how content overflows on the horizontal axis.",
};

<Baseline path="css.properties.overflow-x" />

<Reference category="positioning" name="overflow-x" />

## Usage

```html
<div class="o-x-v ...">Visible</div>
<div class="o-x-auto ...">Auto</div>
<div class="o-x-c ...">Clip</div>
<div class="o-x-h ...">Hidden</div>
<div class="o-x-s ...">Scroll</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="o-x" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="o-x" />

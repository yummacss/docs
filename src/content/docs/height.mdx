export const meta = {
  title: "Height",
  description: "Set the height for an element.",
};

<Baseline path="css.properties.height" />

<Reference category="boxModel" name="height" />

## Usage

```html
<div class="h-16 ..."></div>
<div class="h-full ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="h" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="h" />

### Negative Values

Use negative variants to apply negative height values.

<NegativeValues prefix="h" />

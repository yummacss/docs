export const meta = {
  title: "Border Left Radius",
  description: "Set the left border radius for an element.",
};

<Baseline path="css.properties.border-left-radius" />

<Reference category="border" name="border-left-radius" />

## Usage

Apply a `0.5 rem` radius to the left border of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo blr-2"></div>
</Preview>

```html "blr-2"
<div class="w-16 h-16 bg-indigo blr-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="blr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="blr" />

## Logical Properties

Use logical properties to adapt to the document's writing mode & direction.

### Border Inline Start Radius

Set the logical inline-start border radius for an element.

<Baseline path="css.properties.border-inline-start-radius" />

<Reference category="border" name="border-inline-start-radius" />

<Preview>
  <div className="w-16 h-16 bg-indigo bisr-2"></div>
</Preview>

```html "bisr-2"
<div class="w-16 h-16 bg-indigo bisr-2"></div>
```

export const meta = {
  title: "Right",
  description: "Set the right placement of positioned elements.",
};

<Baseline path="css.properties.right" />

<Reference category="positioning" name="right" />

## Usage

```html "r-0"
<div class="p-r d-18 sm:d-32 c-white">
  <div class="p-a i-0 b-1"></div>
  <div class="p-a b-0 r-0 d-f d-12 ai-c jc-c bg-indigo p-4">A</div>
</div>
```

### Responsive Design

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="r" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="r" />

### Negative Values

Use negative variants to position in the opposite direction.

<NegativeValues prefix="r" />

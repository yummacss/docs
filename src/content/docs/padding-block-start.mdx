export const meta = {
  title: "Padding Block Start",
  description: "Set an element's logical block start padding.",
};

<Baseline path="css.properties.padding-block-start" />

<Reference category="boxModel" name="padding-block-start" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="padding-block-start" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="padding-block-start" />

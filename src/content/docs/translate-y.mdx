export const meta = {
  title: "Translate Y",
  description: "Set the vertical translation for an element.",
};

<Baseline path="css.properties.translate" />

<Reference category="transform" name="translate-y" />

## Usage

Control the vertical translation of an element along the Y-axis.

### Responsive Design

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="t-ty" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="t-ty" />

### Negative Values

Use negative variants to translate in the opposite direction.

<NegativeValues prefix="t-ty" />

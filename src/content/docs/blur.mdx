export const meta = {
  title: "Blur",
  description: "Set the blur filter for an element.",
};

<Baseline path="css.properties.filter" />

<Reference category="effect" name="blur" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="f-b" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="f-b" />

### Negative Values

Use negative variants to apply negative blur values.

<NegativeValues prefix="f-b" />

export const meta = {
  title: "Overscroll Behavior Inline",
  description: "Set the browser's scroll overflow behavior on the inline axis.",
};

<Baseline path="css.properties.overscroll-behavior-inline" />

<Reference category="interactivity" name="overscroll-behavior-inline" />

## Usage

```html
<div class="obi-a ..."></div>
<div class="obi-c ..."></div>
<div class="obi-n ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes.

<ResponsiveVariant prefix="obi" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="obi" />

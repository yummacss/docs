export const meta = {
  title: "Padding Inline Start",
  description: "Control an element's logical inline start padding.",
};

<Baseline path="css.properties.padding-inline-start" />

<Reference category="boxModel" name="padding-inline-start" />

## Usage

<Preview>
  <div id="area">
    <div className="p-8">
      <p className="pis-6 bg-indigo c-white">6</p>
    </div>
  </div>
</Preview>

```html "pis-6"
<div>
  <div class="p-8">
    <p class="pis-6 bg-indigo c-white">6</p>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="pis" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="pis" />

export const meta = {
  title: "Scroll Margin",
  description: "Set the scroll margin for snapping & alignment.",
};

<Baseline path="css.properties.scroll-margin" />

<Reference category="interactivity" name="scroll-margin" />

## Usage

```html
<div class="sm-6 ..."></div>
<div class="smt-6 ..."></div>
<div class="smr-6 ..."></div>
<div class="smb-6 ..."></div>
<div class="sml-6 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="sm" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="sm" />

### Negative Values

Use negative variants to apply negative scroll margins.

<NegativeValues prefix="sm" />

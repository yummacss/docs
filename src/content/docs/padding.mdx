export const meta = {
  title: "Padding",
  description: "Set the padding for an element.",
};

<Baseline path="css.properties.padding" />

## Usage

<Reference category="boxModel" name="padding" />

```html
<div class="p-6 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="p" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="p" />

### Negative Values

Use negative variants to apply negative padding values.

<NegativeValues prefix="p" />

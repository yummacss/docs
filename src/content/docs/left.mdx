export const meta = {
  title: "Left",
  description: "Set the left placement of positioned elements.",
};

<Baseline path="css.properties.left" />

<Reference category="positioning" name="left" />

## Usage

```html "l-0"
<div class="p-r w-18 h-18 c-white sm:w-32 sm:h-32">
  <div class="p-a i-0 b-1"></div>
  <div class="d-f p-a t-0 l-0 ai-c jc-c w-12 h-12 p-4 bg-indigo">A</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="l" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="l" />

### Negative Values

Use negative variants to position in the opposite direction.

<NegativeValues prefix="l" />

export const meta = {
  title: "Cursor",
  description: "Controls the cursor style when hovering over an element.",
};

<ApiTable category="interactivity" name="cursor" />

## Progress

Indicates that a task is in progress.

<Note icon="cursor_arrow">Try hovering over the buttons to see how the cursor transforms with your mouse.</Note>

<Preview className="bg-white">
  <button className="c-pr bg-gray fw-600 px-4 py-2 tc-white rad-1">Sending</button>
</Preview>

```html "c-pr"
<button class="c-pr bg-slate fw-600 px-4 py-2 tc-white rad-1">Sending</button>
```

## Help

Indicates that the element provides helpful information.

<Note icon="cursor_arrow">Try hovering over the buttons to see how the cursor transforms with your mouse.</Note>

<Preview className="bg-white">
  <button className="c-h bg-blue fw-600 px-6 py-2 tc-white rad-1">Help</button>
</Preview>

```html "c-h"
<button class="c-h bg-blue fw-600 px-6 py-2 tc-white rad-1">Help</button>
```

## Not Allowed

Indicates that the requested action cannot be performed.

<Note icon="cursor_arrow">Try hovering over the buttons to see how the cursor transforms with your mouse.</Note>

<Preview className="bg-white">
  <button className="c-na b-2 bg-red fw-600 px-6 py-2 tc-white rad-1">Stop</button>
</Preview>

```html "c-na"
<button class="c-na b-2 bg-red fw-600 px-6 py-2 tc-white rad-1">Stop</button>
```

## Pointer

Indicates that the element is a interactive element that can be activated.

<Note icon="cursor_arrow">Try hovering over the buttons to see how the cursor transforms with your mouse.</Note>

```html "c-p"
<button class="c-p bg-black fw-600 px-6 py-2 tc-white rad-1">Buy</button>
```

## Using utility variants

Learn how to override existing utilities based on the user's [screen size](/docs/media-queries) or other factors, such as [hover states](/docs/pseudo-classes).

### Using media queries

<MediaVariant class="c-p" variant="c-wait" classPrefix="c" />

### Using hover states

<HoverVariant class="c-p" variant="c-wait" classPrefix="c" />

export const meta = {
  title: "Padding Inline End",
  description: "Control an element's logical inline end padding.",
};

<Baseline path="css.properties.padding-inline-end" />

<Reference category="boxModel" name="padding-inline-end" />

## Usage

<Preview>
  <div id="area">
    <div className="p-8">
      <p className="pie-6 bg-indigo c-white">6</p>
    </div>
  </div>
</Preview>

```html "pie-6"
<div>
  <div class="p-8">
    <p class="pie-6 bg-indigo c-white">6</p>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="pie" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="pie" />

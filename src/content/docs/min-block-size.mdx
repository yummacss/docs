export const meta = {
  title: "Min Block Size",
  description: "Control an element's minimum logical block size.",
};

<Baseline path="css.properties.min-block-size" />

<Reference category="boxModel" name="min-block-size" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="min-block-size" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="min-block-size" />

### Negative Values

Use negative variants to apply negative dimensions.

<NegativeValues category="boxModel" name="min-block-size" />

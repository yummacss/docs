export const meta = {
  title: "Padding Bottom",
  description: "Control an element's bottom padding.",
};

<Baseline path="css.properties.padding-bottom" />

<Reference category="boxModel" name="padding-bottom" />

## Usage

```html
<div class="pb-6 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="pb" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="pb" />

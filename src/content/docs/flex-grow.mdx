export const meta = {
  title: "Flex Grow",
  description: "Set how flex items grow to fill available space.",
};

<Baseline path="css.properties.flex-grow" />

<Reference category="flexbox" name="flex-grow" />

## Usage

<Preview variant="inline">
  <div className="d-f g-4 c-white">
    <div className="d-f fg-0 ai-c jc-c p-4 bg-indigo">A</div>
    <div className="d-f fg-1 ai-c jc-c p-4 bg-indigo">B</div>
    <div className="d-f fg-2 ai-c jc-c p-4 bg-indigo">C</div>
  </div>
</Preview>

```html "fg-0" "fg-1" "fg-2"
<div class="d-f g-4 c-white">
  <div class="d-f fg-0 ai-c jc-c p-4 bg-indigo">A</div>
  <div class="d-f fg-1 ai-c jc-c p-4 bg-indigo">B</div>
  <div class="d-f fg-2 ai-c jc-c p-4 bg-indigo">C</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="fg" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="fg" />

### Negative Values

Use negative variants to apply negative flex grow values.

<NegativeValues prefix="fg" />

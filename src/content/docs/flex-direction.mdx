export const meta = {
  title: "Flex Direction",
  description: "Set the direction for flex items.",
};

<Baseline path="css.properties.flex-direction" />

<Reference category="flexbox" name="flex-direction" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="flexbox" name="flex-direction" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="flexbox" name="flex-direction" />

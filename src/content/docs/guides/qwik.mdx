export const meta = {
    title: "Qwik with Yumma CSS",
    description: "Integrate Yumma CSS into Qwik applications.",
};

## Creating a new project

To create a new Qwik project, you need to run the [Qwik CLI](https://qwik.dev/docs/getting-started/#create-an-app-using-the-cli) in your terminal.

<Stepper>
  <Step title="Install Yumma CSS">
    ```bash
    pnpm add yummacss -D
    ```
  </Step>

  <Step title="Initialize configuration">
    Create a configuration file in your project.

    ```bash
    pnpm yummacss init
    ```
  </Step>

  <Step title="Set up configuration">
    Specify the locations of all your project files in the config file.

    ```js title="yumma.config.mjs" mark={2-3}
    export default {
      source: ["./src/**/*.{ts,tsx}"],
      output: "./src/global.css",
      buildOptions: {
        reset: true,
        minify: false,
      }
    };
    ```
  </Step>

  <Step title="Build styles">
    You can now start generating your CSS with the [`build`](/docs/foundations/config/#build) or [`watch`](/docs/foundations/config/#watch) command.

    ```bash
    pnpm yummacss build
    ```
  </Step>

  <Step title="Done!">
    You're all set to start using Yumma CSS utilities in your project.

    ```tsx title="routes/index.tsx" mark={5-7}
    import { component$ } from "@builder.io/qwik";

    export default component$(() => {
      return (
        <div class="bg-indigo-12 d-g h-dvh pi-c tc-white">
          <h1 class="fs-3xl fw-500">Yumma CSS ⚙️ Qwik</h1>
        </div>
      );
    });
    ```
  </Step>
</Stepper>

export const frontmatter = {
    title: "Nuxt.js with Yumma CSS",
    description: "Integrate Yumma CSS into Nuxt.js applications.",
};

## Creating a new project

To create a new Nuxt.js project, you need run the [Nuxt.js command](https://nuxt.com/docs/getting-started/installation#new-project) in your terminal.

<Stepper>
  <Step title="Install Yumma CSS">
    ```bash
    pnpm add yummacss -D
    ```
  </Step>

  <Step title="Initialize configuration">
    Create a configuration file in your project.

    ```bash
    pnpm yummacss init
    ```
  </Step>

  <Step title="Set up configuration">
    Specify the locations of all your project files in the config file.

    ```js title="yumma.config.mjs" mark={2-3}
    export default {
      source: ["./app.vue"],
      output: "./assets/global.css",
      buildOptions: {
        reset: true,
        minify: false,
      }
    };
    ```
  </Step>

  <Step title="Build styles">
    You can now start generating your CSS with the [`build`](/docs/foundations/config/#build) or [`watch`](/docs/foundations/config/#watch) command.

    ```bash
    pnpm yummacss build
    ```
  </Step>

  <Step title="Done!">
    You're all set to start using Yumma CSS utilities in your project.

    ```html title="app.vue" mark={2-4}
    <template>
      <div class="bg-indigo-12 d-g h-dvh pi-c tc-white">
        <h1 class="fs-3xl fw-500">Yumma CSS ⚙️ Nuxt.js</h1>
      </div>
    </template>
    ```
  </Step>
</Stepper>

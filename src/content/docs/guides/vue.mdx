---
title: Yumma CSS and Vue
banner:
  content: Support Yumma CSS by <a href=https://www.yummacss.com/docs/next>starring us on GitHub</a>!
description: Setup Yumma CSS with Vue.
slug: docs/guides/vue
---

## Creating a new project

To create a new Vue project, you need run the [Vue](https://svelte.dev/docs/cli/sv-create) command in your terminal.

<Steps>

    1. **Install Yumma CSS:**

        Install the Yumma CSS Library using a package manager.

        ```bash title="Installing dependencies..." frame="terminal"
        npm install yummacss@latest
        ```

    2. **Include** `yummacss` **in your main CSS file:**

        In this case, we're importing the minified version of Yumma CSS.

        ```css title="src/main.css" mark={1}
        @import "/node_modules/yummacss/dist/yumma.min.css";
        ```

    3. **Start development server**

        You can also run `npm start` to start your application.

        ```bash title="Starting development server..."
        npm run dev
        ```

    4. **Done!**

        You're all set to start using Yumma CSS utility classes in your project.

        ```vue title="App.vue" mark={2}
        <template>
            <h1 class="fs-sm fw-700 ta-c tc-indigo">Yumma CSS 🤝 Vue</h1>
        </template>
        ```

</Steps>

---

## Clone this project

You can clone the project from the [GitHub repository](https://github.com/yumma-lib/vue-example).

```bash title="Cloning the project..."
git clone https://github.com/yumma-lib/vue-example.git
```

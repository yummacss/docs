export const meta = {
  title: "Transition Delay",
  description: "Set the delay before a transition effect starts.",
};

<Baseline path="css.properties.transition-delay" />

<Reference category="transition" name="transition-delay" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="td" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="td" />

### Negative Values

Use negative variants to apply negative transition delay values.

<NegativeValues prefix="td" />

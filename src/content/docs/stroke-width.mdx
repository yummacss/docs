export const meta = {
  title: "Stroke Width",
  description: "Set the width of a stroke in SVG shapes.",
};

<Baseline path="css.properties.stroke-width" />

<Reference category="svg" name="stroke-width" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="svg" name="stroke-width" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="svg" name="stroke-width" />

### Negative Values

Use negative variants to apply negative stroke width values.

<NegativeValues category="svg" name="stroke-width" />

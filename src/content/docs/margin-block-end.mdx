export const meta = {
  title: "Margin Block End",
  description: "Control an element's logical block end margin.",
};

<Baseline path="css.properties.margin-block-end" />

<Reference category="boxModel" name="margin-block-end" />

## Usage

<Preview>
  <div className="d-f fd-r" id="area">
    <div className="d-f t-8 ai-c jc-c w-16 h-16 mbe-8 bg-indigo c-white">8</div>
  </div>
</Preview>

```html "mbe-8"
<div class="d-f fd-r">
  <div class="d-f t-8 ai-c jc-c w-16 h-16 mbe-8 bg-indigo c-white">8</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="mbe" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="mbe" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="mbe" />

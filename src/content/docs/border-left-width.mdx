export const meta = {
  title: "Border Left Width",
  description: "Set the left border width for an element.",
};

<Baseline path="css.properties.border-left-width" />

<Reference category="border" name="border-left-width" />

## Usage

```html
<div class="blw-4 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="blw" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="blw" />

## Logical Properties

Use logical properties to adapt to the document's writing mode & direction.

### Border Inline Start Width

Set the logical inline-start border width for an element.

<Baseline path="css.properties.border-inline-start-width" />

<Reference category="border" name="border-inline-start-width" />

```html
<div class="bisw-4 ..."></div>
```

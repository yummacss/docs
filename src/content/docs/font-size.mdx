export const meta = {
  title: "Font Size",
  description: "Set the font size for an element.",
};

<Baseline path="css.properties.font-size" />

<Reference category="font" name="font-size" />

## Usage

### Extra Small

<Preview variant="inline">
  <p className="c-slate fs-xs ta-c">Aa</p>
</Preview>

```html "fs-xs"
<p class="c-slate fs-xs ta-c">Aa</p>
```

### Small

<Preview variant="inline">
  <p className="c-slate fs-sm ta-c">Aa</p>
</Preview>

```html "fs-sm"
<p class="c-slate fs-sm ta-c">Aa</p>
```

### Medium

> Initial value

<Preview variant="inline">
  <p className="c-slate fs-md ta-c">Aa</p>
</Preview>

```html "fs-md"
<p class="c-slate fs-md ta-c">Aa</p>
```

### Large

<Preview variant="inline">
  <p className="c-slate fs-lg ta-c">Aa</p>
</Preview>

```html "fs-lg"
<p class="c-slate fs-lg ta-c">Aa</p>
```

### Extra Large

<Preview variant="inline">
  <p className="c-slate fs-xl ta-c">Aa</p>
</Preview>

```html "fs-xl"
<p class="c-slate fs-xl ta-c">Aa</p>
```

### XXL

<Preview variant="inline">
  <p className="c-slate fs-xxl ta-c">Aa</p>
</Preview>

```html "fs-xxl"
<p class="c-slate fs-xxl ta-c">Aa</p>
```

### 3XL

<Preview variant="inline">
  <p className="c-slate fs-3xl ta-c">Aa</p>
</Preview>

```html "fs-3xl"
<p class="c-slate fs-3xl ta-c">Aa</p>
```

### 4XL

<Preview variant="inline">
  <p className="c-slate fs-4xl ta-c">Aa</p>
</Preview>

```html "fs-4xl"
<p class="c-slate fs-4xl ta-c">Aa</p>
```

### 5XL

<Preview variant="inline">
  <p className="c-slate fs-5xl ta-c">Aa</p>
</Preview>

```html "fs-5xl"
<p class="c-slate fs-5xl ta-c">Aa</p>
```

### 6XL

<Preview variant="inline">
  <p className="c-slate fs-6xl ta-c">Aa</p>
</Preview>

```html "fs-6xl"
<p class="c-slate fs-6xl ta-c">Aa</p>
```

### 7XL

<Preview variant="inline">
  <p className="c-slate fs-7xl ta-c">Aa</p>
</Preview>

```html "fs-7xl"
<p class="c-slate fs-7xl ta-c">Aa</p>
```

### 8XL

<Preview variant="inline">
  <p className="c-slate fs-8xl ta-c">Aa</p>
</Preview>

```html "fs-8xl"
<p class="c-slate fs-8xl ta-c">Aa</p>
```

### 9XL

<Preview variant="inline">
  <p className="c-slate fs-9xl ta-c">Aa</p>
</Preview>

```html "fs-9xl"
<p class="c-slate fs-9xl ta-c">Aa</p>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="fs" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="fs" />

### Negative Values

Use negative variants to apply negative font size values.

<NegativeValues prefix="fs" />

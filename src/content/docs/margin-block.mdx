export const meta = {
  title: "Margin Block",
  description: "Control an element's vertical margin.",
};

<Baseline path="css.properties.margin-block" />

<Reference category="boxModel" name="margin-block" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="margin-block" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="margin-block" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues category="boxModel" name="margin-block" />

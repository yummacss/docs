export const meta = {
  title: "Margin Block",
  description: "Control an element's vertical margin.",
};

<Baseline path="css.properties.margin-block" />

<Reference category="boxModel" name="margin-block" />

## Usage

<Preview>
  <div className="d-f fd-r">
    <div className="d-f ai-c jc-c w-16 h-16 my-8 bg-indigo c-white">8</div>
  </div>
</Preview>

```html "my-8"
<div class="d-f fd-r">
  <div class="d-f ai-c jc-c w-16 h-16 my-8 bg-indigo c-white">8</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="my" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="my" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="my" />

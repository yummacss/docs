export const meta = {
  title: "Accent Color",
  description: "Set the accent color for form controls.",
};

<Baseline path="css.properties.accent-color" />

<Reference category="color" name="accent-color" />

## Usage

```html
<input class="ac-indigo ..." type="checkbox" checked />
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="ac" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="ac" />

### Opacity Modifiers

Adjust color transparency by appending an opacity suffix.

<OpacityModifier prefix="ac" />

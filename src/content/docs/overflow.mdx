export const meta = {
  title: "Overflow",
  description: "Set how content overflows its container.",
};

<Baseline path="css.properties.overflow" />

<Reference category="positioning" name="overflow" />

## Usage

```html
<div class="o-v ...">Visible</div>
<div class="o-auto ...">Auto</div>
<div class="o-c ...">Clip</div>
<div class="o-h ...">Hidden</div>
<div class="o-s ...">Scroll</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="o" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="o" />

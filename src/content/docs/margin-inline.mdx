export const meta = {
  title: "Margin Inline",
  description: "Control an element's horizontal margin.",
};

<Baseline path="css.properties.margin-inline" />

<Reference category="boxModel" name="margin-inline" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="margin-inline" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="margin-inline" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues category="boxModel" name="margin-inline" />

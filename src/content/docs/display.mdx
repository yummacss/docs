export const meta = {
  title: "Display",
  description: "Set the display box type of an element.",
};

<Baseline path="css.properties.display" />

<Reference category="layout" name="display" />

## Usage

```html
<div class="d-i ...">Inline</div>
<div class="d-b ...">Block</div>
<div class="d-f ...">Flex</div>
<div class="d-fr ...">Flow Root</div>
<div class="d-g ...">Grid</div>
<div class="d-if ...">Inline Flex</div>
<div class="d-ib ...">Inline Block</div>
<div class="d-ig ...">Inline Grid</div>
<div class="d-it ...">Inline Table</div>
<div class="d-t ...">Table</div>
<div class="d-none ...">None</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="d" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="d" />

---
title: Backdrop Blur
description: Set the backdrop blur filter for an element.
---

<Baseline path="css.properties.backdrop-filter" />

<Reference category="effect" name="backdrop-blur" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="effect" name="backdrop-blur" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="effect" name="backdrop-blur" />

### Negative Values

Use negative variants to apply negative backdrop blur values.

<NegativeValues category="effect" name="backdrop-blur" />


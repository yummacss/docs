export const meta = {
  title: "Fill",
  description: "Set the fill color for SVG elements.",
};

<Baseline path="css.properties.fill" />

<Reference category="color" name="fill" />

## Usage

```html
<svg class="f-silver ..."></svg>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="f" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="f" />

### Opacity Modifiers

Adjust color transparency by appending an opacity suffix.

<OpacityModifier prefix="f" />

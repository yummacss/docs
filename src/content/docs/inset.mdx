export const meta = {
  title: "Inset",
  description: "Set the placement of positioned elements.",
};

<Baseline path="css.properties.inset" />

<Reference category="positioning" name="inset" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="positioning" name="inset" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="positioning" name="inset" />

### Negative Values

Use negative variants to position in the opposite direction.

<NegativeValues category="positioning" name="inset" />

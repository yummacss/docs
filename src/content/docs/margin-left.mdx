export const meta = {
  title: "Margin Left",
  description: "Control an element's left margin.",
};

<Baseline path="css.properties.margin-left" />

<Reference category="boxModel" name="margin-left" />

## Usage

```html
<div class="ml-8 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="ml" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="ml" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="ml" />

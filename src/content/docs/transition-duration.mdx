export const meta = {
  title: "Transition Duration",
  description: "Set the amount of time a transition takes to complete.",
};

<Baseline path="css.properties.transition-duration" />

<Reference category="transition" name="transition-duration" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="transition" name="transition-duration" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="transition" name="transition-duration" />

### Negative Values

Use negative variants to apply negative transition duration values.

<NegativeValues category="transition" name="transition-duration" />

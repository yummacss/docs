export const meta = {
  title: "Margin Block Start",
  description: "Control an element's logical block start margin.",
};

<Baseline path="css.properties.margin-block-start" />

<Reference category="boxModel" name="margin-block-start" />

## Usage

```html
<div class="mbs-8 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="mbs" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="mbs" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="mbs" />

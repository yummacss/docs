export const meta = {
  title: "Grid Row Start",
  description: "Set where a grid item begins within the grid rows.",
};

<Baseline path="css.properties.grid-row-start" />

<Reference category="grid" name="grid-row-start" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="grid" name="grid-row-start" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="grid" name="grid-row-start" />

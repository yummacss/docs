export const meta = {
  title: "Translate",
  description: "Set the translation for an element.",
};

<Baseline path="css.properties.translate" />

<Reference category="transform" name="translate" />

## Usage

Control the translation of an element. This utility sets both the horizontal & vertical translation.

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="t-t" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="t-t" />

### Negative Values

Use negative variants to translate in the opposite direction.

<NegativeValues prefix="t-t" />

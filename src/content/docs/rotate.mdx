export const meta = {
  title: "Rotate",
  description: "Set the rotation for an element.",
};

<Baseline path="css.properties.rotate" />

<Reference category="transform" name="rotate" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="transform" name="rotate" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="transform" name="rotate" />

### Negative Values

Use negative variants to rotate in the opposite direction.

<NegativeValues category="transform" name="rotate" />

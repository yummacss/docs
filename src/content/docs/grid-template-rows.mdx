export const meta = {
  title: "Grid Template Rows",
  description: "Set the rows for a grid layout.",
};

<Baseline path="css.properties.grid-template-rows" />

<Reference category="grid" name="grid-template-rows" />

## Usage

Define the rows of a grid layout.

<Preview variant="inline">
    <div className="o-auto p-r">
      <div className="d-g g-4 gaf-c gtr-3 c-white ta-c" id="area">
          <div className="p-4 bg-indigo">A</div>
          <div className="p-4 bg-indigo">B</div>
          <div className="p-4 bg-indigo">C</div>
          <div className="p-4 bg-indigo">D</div>
          <div className="p-4 bg-indigo">E</div>
          <div className="p-4 bg-indigo">F</div>
          <div className="p-4 bg-indigo">G</div>
          <div className="p-4 bg-indigo">H</div>
      </div>
    </div>
</Preview>

```html "gtr-3"
<div class="o-auto p-r">
  <div class="d-g g-4 gaf-c gtr-3 c-white ta-c">
    <div class="p-4 bg-indigo">A</div>
    <div class="p-4 bg-indigo">B</div>
    <div class="p-4 bg-indigo">C</div>
    <div class="p-4 bg-indigo">D</div>
    <div class="p-4 bg-indigo">E</div>
    <div class="p-4 bg-indigo">F</div>
    <div class="p-4 bg-indigo">G</div>
    <div class="p-4 bg-indigo">H</div>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="gtr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="gtr" />

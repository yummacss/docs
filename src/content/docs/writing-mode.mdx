export const meta = {
  title: "Writing Mode",
  description: "Set whether lines of text are laid out horizontally or vertically.",
};

<Baseline path="css.properties.writing-mode" />

<Reference category="text" name="writing-mode" />

## Usage

```html
<div class="wm-htb ...">Horizontal Top to Bottom</div>
<div class="wm-vrl ...">Vertical Right to Left</div>
<div class="wm-vlr ...">Vertical Left to Right</div>
<div class="wm-srl ...">Sideways Right to Left</div>
<div class="wm-slr ...">Sideways Left to Right</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="wm" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="wm" />

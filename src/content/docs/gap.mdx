export const meta = {
  title: "Gap",
  description: "Set the spacing between flex & grid items.",
};

<Baseline path="css.properties.gap" />

<Reference category="grid" name="gap" />

## Usage

Define uniform spacing between rows and columns.

<Preview variant="inline">
  <div className="d-g g-10 gtc-3 c-white">
    <div className="d-f ai-c jc-c p-4 bg-indigo">A</div>
    <div className="d-f ai-c jc-c p-4 bg-indigo">B</div>
    <div className="d-f ai-c jc-c p-4 bg-indigo">C</div>
    <div className="d-f ai-c jc-c p-4 bg-indigo">D</div>
    <div className="d-f ai-c jc-c p-4 bg-indigo">E</div>
  </div>
</Preview>

```html "g-10"
<div class="d-g g-10 gtc-3 c-white">
  <div class="d-f ai-c jc-c p-4 bg-indigo">A</div>
  <div class="d-f ai-c jc-c p-4 bg-indigo">B</div>
  <div class="d-f ai-c jc-c p-4 bg-indigo">C</div>
  <div class="d-f ai-c jc-c p-4 bg-indigo">D</div>
  <div class="d-f ai-c jc-c p-4 bg-indigo">E</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="g" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="g" />

### Negative Values

Use negative variants to apply negative spacing.

<NegativeValues prefix="g" />

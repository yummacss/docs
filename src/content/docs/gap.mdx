export const meta = {
  title: "Gap",
  description: "Set the spacing between flex & grid items.",
};

<Baseline path="css.properties.gap" />

<Reference category="grid" name="gap" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="grid" name="gap" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="grid" name="gap" />

### Negative Values

Use negative variants to apply negative spacing.

<NegativeValues category="grid" name="gap" />

---
title: Font Weight
description: Set the font weight for an element.
---

<Baseline path="css.properties.font-weight" />

<Reference category="font" name="font-weight" />

<Hint icon="heart">
  Yumma CSS uses the value `600` for all headings. Learn more about it in the
  [normalize docs](/docs/normalize).
</Hint>

<Hint icon="heart">Yumma CSS uses the value `700` for elements like `<b>` and `<strong>`. Learn more about it in the [normalize docs](/docs/normalize).</Hint>

### Media Queries

Prefix a class with a breakpoint to apply it from that width up.

<Reference category="font" name="font-weight" variant="media" />

### Pseudo Classes

Prefix a class with a state to apply it only while that state matches.

<Reference category="font" name="font-weight" variant="pseudo-class" />


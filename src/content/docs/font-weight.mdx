export const meta = {
  title: "Font Weight",
  description: "Set the font weight for an element.",
};

<Baseline path="css.properties.font-weight" />

<Reference category="font" name="font-weight" />

<Hint icon="heart">
  Yumma CSS uses the value `600` for all headings. Learn more about it in the
  [base styles docs](/docs/base-styles).
</Hint>

<Hint icon="heart">Yumma CSS uses the value `700` for elements like `<b>` and `<strong>`. Learn more about it in the [base styles docs](/docs/base-styles).</Hint>

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="font" name="font-weight" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="font" name="font-weight" />

export const meta = {
  title: "Clear",
  description: "Set how an element behaves in relation to floating elements.",
};

<Baseline path="css.properties.clear" />

<Reference category="layout" name="clear" />

## Usage

```html
<div class="cl-none ..."></div>
<div class="cl-b ..."></div>
<div class="cl-ie ..."></div>
<div class="cl-is ..."></div>
<div class="cl-l ..."></div>
<div class="cl-r ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="cl" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="cl" />

export const meta = {
  title: "Place Self",
  description: "Set individual item justification & alignment simultaneously.",
};

<Baseline path="css.properties.place-self" />

<Reference category="grid" name="place-self" />

## Usage

```html
<div class="ps-auto ..."></div>
<div class="ps-c ..."></div>
<div class="ps-e ..."></div>
<div class="ps-s ..."></div>
<div class="ps-st ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="ps" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="ps" />

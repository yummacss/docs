export const meta = {
  title: "Inline Size",
  description: "Control an element's logical inline size.",
};

<Baseline path="css.properties.inline-size" />

<Reference category="boxModel" name="inline-size" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="inline-size" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="inline-size" />

### Negative Values

Use negative variants to apply negative dimensions.

<NegativeValues category="boxModel" name="inline-size" />

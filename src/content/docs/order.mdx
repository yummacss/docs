export const meta = {
  title: "Order",
  description: "Controls the order of flex and grid items.",
};

<Baseline path="css.properties.order" />

<ApiTable category="flexbox" name="order" />

This example showcases various `order` utilities:

- The **or-l** order utility sets the order property to **-9999**, placing the item at the beginning of the flex container.
- The **or-f** order utility sets the order property to **9999**, placing the item at the end of the flex container.

<Preview variant="inline">
  <div className="d-f jc-sb tc-white">
    <div className="ai-c bg-indigo d-f d-16 jc-c or-f">A</div>
    <div className="ai-c bg-indigo d-f d-16 jc-c">B</div>
    <div className="ai-c bg-indigo d-f d-16 jc-c or-l">C</div>
  </div>
</Preview>

```html "or-f"
<div class="d-f jc-sb tc-white">
  <div class="ai-c bg-indigo d-f d-16 jc-c or-f">A</div>
  <div class="ai-c bg-indigo d-f d-16 jc-c">B</div>
  <div class="ai-c bg-indigo d-f d-16 jc-c or-l">C</div>
</div>
```

## Using utility variants

Learn how to override existing utilities based on the user's [screen size](/docs/media-queries) or other factors, such as [hover states](/docs/pseudo-classes).

### Targeting different viewports

<MediaVariant class="or-f" variant="or-l" classPrefix="or" />

### Targeting hover states

<HoverVariant class="or-f" variant="or-l" classPrefix="or" />

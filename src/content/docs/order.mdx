export const meta = {
  title: "Order",
  description: "Set the order for flex & grid items.",
};

<Baseline path="css.properties.order" />

<Reference category="flexbox" name="order" />

## Usage

<Preview variant="inline">
  <div className="d-f jc-sb c-white">
    <div className="d-f ai-c jc-c or-f w-16 h-16 bg-indigo">A</div>
    <div className="d-f ai-c jc-c w-16 h-16 bg-indigo">B</div>
    <div className="d-f ai-c jc-c or-l w-16 h-16 bg-indigo">C</div>
  </div>
</Preview>

```html "or-f"
<div class="d-f jc-sb c-white">
  <div class="d-f ai-c jc-c or-f w-16 h-16 bg-indigo">A</div>
  <div class="d-f ai-c jc-c w-16 h-16 bg-indigo">B</div>
  <div class="d-f ai-c jc-c or-l w-16 h-16 bg-indigo">C</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="or" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="or" />

### Negative Values

Use negative variants to apply negative order values.

<NegativeValues prefix="or" />

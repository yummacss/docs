export const meta = {
  title: "Order",
  description: "Set the order for flex & grid items.",
};

<Baseline path="css.properties.order" />

<Reference category="flexbox" name="order" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="or" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="or" />

### Negative Values

Use negative variants to apply negative order values.

<NegativeValues prefix="or" />

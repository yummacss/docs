export const meta = {
  title: "List Style Type",
  description: "Set the marker style of a list.",
};

<Baseline path="css.properties.list-style-type" />

<Reference category="text" name="list-style-type" />

## Usage

```html
<ul class="lst-d ...">Disc</ul>
<ul class="lst-c ...">Circle</ul>
<ul class="lst-s ...">Square</ul>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="lst" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="lst" />

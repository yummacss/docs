export const meta = {
  title: "Max Block Size",
  description: "Control an element's maximum logical block size.",
};

<Baseline path="css.properties.max-block-size" />

<Reference category="boxModel" name="max-block-size" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="max-block-size" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="max-block-size" />

### Negative Values

Use negative variants to apply negative dimensions.

<NegativeValues category="boxModel" name="max-block-size" />

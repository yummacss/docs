export const meta = {
  title: "Padding Left",
  description: "Control an element's left padding.",
};

<Baseline path="css.properties.padding-left" />

<Reference category="boxModel" name="padding-left" />

## Usage

<Preview>
  <div id="area">
    <div className="p-8">
      <p className="pl-6 bg-indigo c-white">6</p>
    </div>
  </div>
</Preview>

```html "pl-6"
<div>
  <div class="p-8">
    <p class="pl-6 bg-indigo c-white">6</p>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="pl" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="pl" />

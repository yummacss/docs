export const meta = {
  title: "Padding Left",
  description: "Set an element's left padding.",
};

<Baseline path="css.properties.padding-left" />

<Reference category="boxModel" name="padding-left" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="padding-left" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="padding-left" />

export const meta = {
  title: "Margin Top",
  description: "Control an element's top margin.",
};

<Baseline path="css.properties.margin-top" />

<Reference category="boxModel" name="margin-top" />

## Usage

<Preview>
  <div className="d-f fd-r" id="area">
    <div className="d-f t-8 ai-c jc-c w-16 h-16 mt-8 bg-indigo c-white">8</div>
  </div>
</Preview>

```html "mt-8"
<div class="d-f fd-r">
  <div class="d-f t-8 ai-c jc-c w-16 h-16 mt-8 bg-indigo c-white">8</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="mt" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="mt" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="mt" />

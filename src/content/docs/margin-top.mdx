export const meta = {
  title: "Margin Top",
  description: "Control an element's top margin.",
};

<Baseline path="css.properties.margin-top" />

<Reference category="boxModel" name="margin-top" />

## Usage

```html
<div class="mt-8 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="mt" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="mt" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="mt" />

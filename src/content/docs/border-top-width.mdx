export const meta = {
  title: "Border Top Width",
  description: "Set the top border width for an element.",
};

<Baseline path="css.properties.border-top-width" />

<Reference category="border" name="border-top-width" />

## Usage

```html
<div class="btw-4 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="btw" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="btw" />

## Logical Properties

Use logical properties to adapt to the document's writing mode & direction.

### Border Block Start Width

Set the logical block-start border width for an element.

<Baseline path="css.properties.border-block-start-width" />

<Reference category="border" name="border-block-start-width" />

```html
<div class="bbsw-4 ..."></div>
```

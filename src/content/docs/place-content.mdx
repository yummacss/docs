export const meta = {
  title: "Place Content",
  description: "Set the content justification & alignment simultaneously.",
};

<Baseline path="css.properties.place-content" />

<Reference category="grid" name="place-content" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="grid" name="place-content" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="grid" name="place-content" />

export const meta = {
  title: "Font Family",
  description: "Controls the font family of an element.",
};

<ApiTable category="font" name="font-family" />

## Charter

If Charter is not available, it will fall back to `Cambria`, and then to a generic `serif` font.

<Preview className="bg-white" variant="inline">
  <p className="ff-c fs-lg ta-c tc-slate">Sphinx of black quartz, judge my vow.</p>
</Preview>

```html "ff-c"
<p class="ff-c fs-lg ta-c tc-slate">Sphinx of black quartz, judge my vow.</p>
```

## UI Monospace

If ui-monospace is not available, it will fall back to `Consolas`, and then to a generic `monospace` font.

<Preview className="bg-white" variant="inline">
  <p className="ff-m fs-lg ta-c tc-slate">Sphinx of black quartz, judge my vow.</p>
</Preview>

```html "ff-m"
<p class="ff-m fs-lg ta-c tc-slate">Sphinx of black quartz, judge my vow.</p>
```

## System UI

If system-ui is not available, it will fall back to a generic `sans-serif` font.

<Note icon="heart">Yumma CSS uses the **fontSystem** as a default font family. Learn more about it in the [docs](/docs/base-styles#document).</Note>

<Preview className="bg-white" variant="inline">
  <p className="ff-s fs-lg ta-c tc-slate">Sphinx of black quartz, judge my vow.</p>
</Preview>

```html "ff-s"
<p class="ff-s fs-lg ta-c tc-slate">Sphinx of black quartz, judge my vow.</p>
```

## Using utility variants

Learn how to override existing utilities based on the user's [screen size](/docs/media-queries) or other factors, such as [hover states](/docs/pseudo-classes).

### Using media queries

<MediaVariant class="ff-s" variant="ff-m" classPrefix="ff" />

### Using hover states

<HoverVariant class="ff-s" variant="ff-m" classPrefix="ff" />

---
title: Font Family
description: Set the font family for an element.
---

<Baseline path="css.properties.font-family" />

<Reference category="font" name="font-family" />

<Hint icon="heart">
  Yumma CSS uses the `Default` as a default font family. Learn more about it in
  the [docs](/docs/normalize#document).
</Hint>

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="font" name="font-family" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="font" name="font-family" />


export const meta = {
  title: "Border Block Start Radius",
  description: "Set the logical block-start border radius for an element.",
};

<Baseline path="css.properties.border-block-start-radius" />

<Reference category="border" name="border-block-start-radius" />

## Usage

Apply a `0.5 rem` radius to the logical block-start border of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo bbsr-2"></div>
</Preview>

```html "bbsr-2"
<div class="w-16 h-16 bg-indigo bbsr-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bbsr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bbsr" />

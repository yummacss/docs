export const meta = {
  title: "Row Gap",
  description: "Set the spacing between rows in a grid layout.",
};

<Baseline path="css.properties.row-gap" />

<Reference category="grid" name="row-gap" />

## Usage

Define the spacing between grid rows.

<Preview variant="inline">
  <div id="area">
    <div className="rg-10 d-g gtr-3 c-white">
      <div className="bg-indigo p-4 ta-c">A</div>
      <div className="bg-indigo p-4 ta-c">B</div>
      <div className="bg-indigo p-4 ta-c">C</div>
    </div>
  </div>
</Preview>

```html "rg-10"
<div class="rg-10 d-g gtr-3 c-white">
  <div class="bg-indigo p-4 ta-c">A</div>
  <div class="bg-indigo p-4 ta-c">B</div>
  <div class="bg-indigo p-4 ta-c">C</div>
</div>
```

### Responsive Design

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="rg" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="rg" />

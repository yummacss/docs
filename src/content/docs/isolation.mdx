export const meta = {
  title: "Isolation",
  description: "Controls whether an element must create a new stacking context.",
};

<Baseline path="css.properties.isolation" />

<UtilityTable category="positioning" name="isolation" />

## Auto

> Initial value

The element will use the default isolation behavior, allowing overlapping elements to blend together.

```html "is-auto"
<div class="is-auto ..."></div>
```

## Isolate

The element will create a new stacking context, preventing overlapping elements from blending with the background.

```html "is-i"
<div class="is-i ..."></div>
```

## Using utility variants

Learn how to override existing utilities based on the user's [screen size](/docs/media-queries) or other factors, such as [hover states](/docs/pseudo-classes).

### Targeting different viewports

<MediaVariant prefix="is" />

### Targeting hover states

<MediaVariant prefix="is" />

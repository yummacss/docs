export const meta = {
  title: "Isolation",
  description: "Set whether an element must create a new stacking context.",
};

<Baseline path="css.properties.isolation" />

<Reference category="layout" name="isolation" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="layout" name="isolation" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="layout" name="isolation" />

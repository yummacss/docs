export const meta = {
  title: "Line Height",
  description: "Set the line height of an element.",
};

<Baseline path="css.properties.line-height" />

<Reference category="text" name="line-height" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="text" name="line-height" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="text" name="line-height" />

### Negative Values

Use negative variants to translate in the opposite direction.

<NegativeValues category="text" name="line-height" />

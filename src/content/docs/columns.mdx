export const meta = {
  title: "Columns",
  description: "Set the number of columns within an element.",
};

<Baseline path="css.properties.columns" />

<Reference category="layout" name="columns" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="layout" name="columns" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="layout" name="columns" />

### Negative Values

Use negative variants to apply negative column values.

<NegativeValues category="layout" name="columns" />

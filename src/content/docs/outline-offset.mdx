export const meta = {
  title: "Outline Offset",
  description: "Set the offset for an element's outline.",
};

<Baseline path="css.properties.outline-offset" />

<Reference category="outline" name="outline-offset" />

## Usage

```html
<div class="oo-1 ..."></div>
<div class="oo-2 ..."></div>
<div class="oo-3 ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="oo" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="oo" />

### Negative Values

Use negative variants to apply negative outline offset.

<NegativeValues prefix="oo" />

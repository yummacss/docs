export const meta = {
  title: "Border Bottom Left Radius",
  description: "Set the bottom-left border radius for an element.",
};

<Baseline path="css.properties.border-bottom-left-radius" />

<Reference category="border" name="border-bottom-left-radius" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bblr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bblr" />

## Logical Properties

Use logical properties to adapt to the document's writing mode & direction.

### Border End Start Radius

Set the logical end-start border radius for an element.

<Baseline path="css.properties.border-end-start-radius" />

<Reference category="border" name="border-end-start-radius" />

```html
<div class="besr-md ..."></div>
```

export const meta = {
  title: "Skew Y",
  description: "Set the vertical skew for an element.",
};

<Baseline path="css.properties.transform" />

<Reference category="transform" name="skew-y" />

## Usage

```html
<img class="t-sky-3 ..." src="..." />
<img class="t-sky-6 ..." src="..." />
<img class="t-sky-12 ..." src="..." />
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="t-sky" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="t-sky" />

### Negative Values

Use negative variants to skew in the opposite direction.

<NegativeValues prefix="t-sky" />

export const meta = {
  title: "Scroll Snap Align",
  description: "Set the snap alignment of an element.",
};

<Baseline path="css.properties.scroll-snap-align" />

<Reference category="interactivity" name="scroll-snap-align" />

## Usage

```html
<div class="ssa-c ..."></div>
<div class="ssa-e ..."></div>
<div class="ssa-s ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="ssa" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="ssa" />

export const meta = {
  title: "Border End End Radius",
  description: "Set the logical end-end border radius for an element.",
};

<Baseline path="css.properties.border-end-end-radius" />

<Reference category="border" name="border-end-end-radius" />

## Usage

Apply a `0.5 rem` radius to the logical end-end border corner of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo beer-2"></div>
</Preview>

```html "beer-2"
<div class="w-16 h-16 bg-indigo beer-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="beer" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="beer" />

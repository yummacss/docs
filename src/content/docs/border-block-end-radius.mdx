export const meta = {
  title: "Border Block End Radius",
  description: "Set the logical block-end border radius for an element.",
};

<Baseline path="css.properties.border-block-end-radius" />

<Reference category="border" name="border-block-end-radius" />

## Usage

Apply a `0.5 rem` radius to the logical block-end border of an element.

<Preview>
  <div className="w-16 h-16 bg-indigo bber-2"></div>
</Preview>

```html "bber-2"
<div class="w-16 h-16 bg-indigo bber-2"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bber" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bber" />

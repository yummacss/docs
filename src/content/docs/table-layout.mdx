export const meta = {
  title: "Table Layout",
  description: "Set the table cell layout algorithm.",
};

<Baseline path="css.properties.table-layout" />

<Reference category="table" name="table-layout" />

## Usage

### Auto

Allow the browser to determine column widths based on content.

<Preview>
  <table className="w-full c-slate bw-1 bs-d bc-c tl-auto">
    <thead>
      <tr>
        <th className="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Full Name</th>
        <th className="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Phone Number</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td className="p-3 bc-slate bw-1 bs-d ta-c">John Doe</td>
        <td className="p-3 bc-slate bw-1 bs-d ta-c">+1 (555) 123-4567</td>
      </tr>
    </tbody>
  </table>
</Preview>

```html "tl-auto"
<table class="w-full c-slate bw-1 bs-d bc-c tl-auto">
  <thead>
    <tr>
      <th class="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Full Name</th>
      <th class="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Phone Number</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="p-3 bc-slate bw-1 bs-d ta-c">John Doe</td>
      <td class="p-3 bc-slate bw-1 bs-d ta-c">+1 (555) 123-4567</td>
    </tr>
  </tbody>
</table>
```

### Fixed

Set column widths based on the table width and first row.

<Preview>
  <table className="w-full c-slate tl-f">
    <thead>
      <tr>
        <th className="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Full Name</th>
        <th className="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Phone Number</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td className="p-3 bc-slate bw-1 bs-d ta-c">John Doe</td>
        <td className="p-3 bc-slate bw-1 bs-d ta-c">+1 (555) 123-4567</td>
      </tr>
    </tbody>
  </table>
</Preview>

```html "tl-f"
<table class="w-full c-slate bw-1 bs-d bc-c tl-f">
  <thead>
    <tr>
      <th class="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Full Name</th>
      <th class="p-3 bg-silver-2 bc-slate bw-1 bs-d ta-c">Phone Number</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="p-3 bc-slate bw-1 bs-d ta-c">John Doe</td>
      <td class="p-3 bc-slate bw-1 bs-d ta-c">+1 (555) 123-4567</td>
    </tr>
  </tbody>
</table>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="tl" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="tl" />

export const meta = {
  title: "Table Layout",
  description: "Set the table cell layout algorithm.",
};

<Baseline path="css.properties.table-layout" />

<Reference category="layout" name="table-layout" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="layout" name="table-layout" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="layout" name="table-layout" />

export const meta = {
  title: "Background Repeat",
  description: "Control how a background image repeats within an element.",
};

<Baseline path="css.properties.background-repeat" />

<Reference category="background" name="background-repeat" />

## Usage

### Repeat

The background image repeats horizontally & vertically.

<Preview id="background-repeat-repeat" />

```tsx registry="background-repeat-repeat"
```

The background image does not repeat.

<Preview id="background-repeat-no-repeat" />

```tsx registry="background-repeat-no-repeat"
```

The background image repeats & resizes to fit the area.

<Preview id="background-repeat-round" />

```tsx registry="background-repeat-round"
```

The background image repeats horizontally only.

<Preview id="background-repeat-repeat-x" />

```tsx registry="background-repeat-repeat-x"
```

The background image repeats vertically only.

<Preview id="background-repeat-repeat-y" />

```tsx registry="background-repeat-repeat-y"
```

The background image repeats & spaces evenly.

<Preview id="background-repeat-space" />

```tsx registry="background-repeat-space"
```

### Responsive Design

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="br" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="br" />

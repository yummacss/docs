export const meta = {
  title: "Background Repeat",
  description: "Set the repeat behavior for background images.",
};

<Baseline path="css.properties.background-repeat" />

<Reference category="background" name="background-repeat" />

## Usage

```html
<div class="br-r ..."></div>
<div class="br-nr ..."></div>
<div class="br-ro ..."></div>
<div class="br-rx ..."></div>
<div class="br-ry ..."></div>
<div class="br-s ..."></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="br" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="br" />

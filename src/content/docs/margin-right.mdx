export const meta = {
  title: "Margin Right",
  description: "Control an element's right margin.",
};

<Baseline path="css.properties.margin-right" />

<Reference category="boxModel" name="margin-right" />

## Usage

<Preview>
  <div>
    <div className="d-f ai-c jc-c w-16 h-16 mr-8 bg-indigo c-white">8</div>
  </div>
</Preview>

```html "mr-8"
<div>
  <div class="d-f ai-c jc-c w-16 h-16 mr-8 bg-indigo c-white">8</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="mr" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="mr" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues prefix="mr" />

export const meta = {
  title: "Margin Right",
  description: "Set an element's right margin.",
};

<Baseline path="css.properties.margin-right" />

<Reference category="boxModel" name="margin-right" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="boxModel" name="margin-right" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="boxModel" name="margin-right" />

### Negative Values

Use negative variants to apply negative margins.

<NegativeValues category="boxModel" name="margin-right" />

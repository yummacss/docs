export const meta = {
  title: "Flex",
  description: "Set how flex items grow & shrink.",
};

<Baseline path="css.properties.flex" />

<Reference category="flexbox" name="flex" />

## Usage

<Preview variant="inline">
  <div className="c-white">
    <div className="d-f g-4 h-24 w-full">
      <div className="d-f ai-c f-none jc-c p-4 bg-indigo">A</div>
      <div className="d-f ai-c f-1 jc-c p-4 bg-indigo">B</div>
      <div className="d-f ai-c f-auto jc-c p-4 bg-indigo">C</div>
    </div>
  </div>
</Preview>

```html "f-none" "f-1" "f-auto"
<div class="c-white">
  <div class="d-f g-4 h-24 w-full">
    <div class="d-f ai-c f-none jc-c p-4 bg-indigo">A</div>
    <div class="d-f ai-c f-1 jc-c p-4 bg-indigo">B</div>
    <div class="d-f ai-c f-auto jc-c p-4 bg-indigo">C</div>
  </div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="f" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="f" />

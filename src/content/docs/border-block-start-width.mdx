export const meta = {
  title: "Border Block Start Width",
  description: "Set the logical block-start border width for an element.",
};

<Baseline path="css.properties.border-block-start-width" />

<Reference category="border" name="border-block-start-width" />

## Usage

Apply a `0.25 rem` logical block-start border width to an element.

<Preview>
  <div className="w-16 h-16 bc-indigo bs-s bbsw-4"></div>
</Preview>

```html "bbsw-4"
<div class="w-16 h-16 bc-indigo bs-s bbsw-4"></div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="bbsw" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="bbsw" />

export const meta = {
  title: "Column Gap",
  description: "Set the spacing between columns in a grid layout.",
};

<Baseline path="css.properties.column-gap" />

<Reference category="grid" name="column-gap" />

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant category="grid" name="column-gap" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant category="grid" name="column-gap" />

### Negative Values

Use negative variants to apply negative spacing.

<NegativeValues category="grid" name="column-gap" />

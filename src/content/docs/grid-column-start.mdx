export const meta = {
  title: "Grid Column Start",
  description: "Control the starting grid line for a grid item.",
};

<Baseline path="css.properties.grid-column-start" />

<Reference category="grid" name="grid-column-start" />

## Usage

Position the item to start in the third column of the grid.

<Preview variant="inline">
    <div className="d-g g-4 gcs-3 gtc-4 c-white ta-c">
      <div className="p-4 bg-indigo">A</div>
      <div className="p-4 bg-indigo">B</div>
      <div className="p-4 bg-indigo">C</div>
    </div>
</Preview>

```html "gcs-3"
<div class="d-g g-4 gcs-3 gtc-4 c-white ta-c">
  <div className="p-4 bg-indigo">A</div>
  <div className="p-4 bg-indigo">B</div>
  <div className="p-4 bg-indigo">C</div>
</div>
```

### Responsiveness

Use breakpoint prefixes to target different screen sizes. Styles apply from the breakpoint & up.

Target viewport sizes with breakpoint prefixes.

<ResponsiveVariant prefix="gcs" />

### Hover State

Apply styles conditionally on hover using the `h:` prefix.

<HoverVariant prefix="gcs" />

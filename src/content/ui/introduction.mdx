import { uiSidebarConfig } from "@/utils/ui-sidebar";
import Link from "next/link";

export const meta = {
   title: "Introduction",
   description: "Yumma UI is a set of beautiful UI components designed with Yumma CSS.",
};

## Why Yumma UI?

With Yumma UI, you will get access to dozens of free production-ready components and blocks for whatever you're building. 

All of our components follows [APG patterns](https://www.w3.org/WAI/ARIA/apg/patterns/) which means they are accessible and keyboard navigable.

---

## Installation

Yumma UI is built on Yumma CSS, so you'll want to make sure you've got the latest version of Yumma CSS installed.

Don't know how to install Yumma CSS? Check out the [installation](/docs/installation) page.

```bash title="Terminal"
npm install yummacss@latest -D
```

---

## Components

A list of all available components:

<div className="d-g gtc-1 sm:gtc-3 rg-8 my-10">
  {uiSidebarConfig[1].items.map(item => (
    <div key={item.slug}>
      <Link className="h:td-u tc-white" href={`/ui/${item.slug}`}>{item.title}</Link>
    </div>
  ))}
</div>

---

## Blocks

A list of all available blocks:

<div className="d-g gtc-1 sm:gtc-3 rg-8 my-10">
  {uiSidebarConfig[2].items.map(item => (
    <div key={item.slug}>
      <Link className="h:td-u tc-white" href={`/ui/${item.slug}`}>{item.title}</Link>
    </div>
  ))}
</div>

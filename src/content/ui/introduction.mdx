import Link from "next/link";
import { uiSidebarConfig } from "@/utils/ui-sidebar";

export const meta = {
   title: "Introduction",
   description: "A set of beautiful UI components designed with Yumma CSS that you can simply copy and paste into your application.",
};

## Why Yumma UI?

With Yumma UI, you will get access to dozens of free UI blocks, accessible HTML components for whatever you're building. They are the perfect starting point for your next idea, while giving you the freedom of customization.

## Components

A list of all available components:

<div className="d-g gtc-1 sm:gtc-3 rg-8 my-10">
  {uiSidebarConfig[1].items.map(item => (
    <div key={item.slug}>
      <Link className="h:td-u tc-white" href={`/ui/${item.slug}`}>{item.title}</Link>
    </div>
  ))}
</div>
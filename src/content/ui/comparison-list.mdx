export const meta = {
  title: "Comparison List",
  description: "Displays features or products side-by-side.",
};

## Examples

### Basic

<Preview id="comparison-list-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Side by Side

<Preview id="comparison-list-side-by-side" />

```tsx
// wip: reuse registry content to avoid mismatches
```

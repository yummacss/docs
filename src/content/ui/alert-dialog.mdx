export const meta = {
  title: "Alert Dialog",
  description: "A dialog that requires a user response to proceed.",
};

## Examples

<Preview id="alert-dialog-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Menu

Use the `onClick` handler on the menu item to open the dialog imperatively.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Close confirmation

This example shows a confirmation dialog that opens if the text is discarded.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Detached triggers

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Multiple triggers

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Multiple trigger control

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/alert-dialog) documentation for more information.
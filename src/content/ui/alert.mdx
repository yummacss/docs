export const meta = {
   title: "Alert",
   description: "A component that displays important messages and notifications to users.",
};

## Example

A component that displays important messages and notifications to users.

```tsx mark={1}
import { Alert, AlertContent, AlertTitle, AlertDescription } from "@yummacss/react";
import { InfoCircledIcon } from "@radix-ui/react-icons";

<Alert>
  <AlertContent>
    <InfoCircledIcon />
    <AlertTitle>Update available</AlertTitle>
    <AlertDescription>A new version of Yumma UI is available.</AlertDescription>
  </AlertContent>
</Alert>
```

## API reference

### `variant`

> Default value is `base`

```ts '"base"'
(property) variant?: "base" | "error" | "info" | "success" | "warning" | null | undefined
```

## Custom styling

Apply custom styles to the `<Alert>` component.

```tsx 'className="..."'
<Alert className="...">...</Alert>
```

## Extend properties

Extend the `<Alert>` component with the native `<div>` HTML attributes.

```tsx "{...props}"
<Alert {...props}>...</Alert>
```

<br />

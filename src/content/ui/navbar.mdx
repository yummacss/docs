export const meta = {
  title: "Navbar",
  description: "Provides primary site navigation with links to main sections and pages.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This example shows how to use Yumma CSS and Base UI to create an navbar block.

<Preview id="navbar-basic" variant="noPadding" />

```tsx registry="navbar-basic"
```

### Center aligned links

<Preview id="navbar-center-aligned-links" variant="noPadding" />

```tsx registry="navbar-center-aligned-links"
```


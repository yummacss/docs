export const meta = {
  title: "Form",
  description: "A native form element with consolidated error handling.",
};

## Examples

### Basic

A basic Form implementation.

<Preview id="form-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Validation

Add native form validation.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Tooltip

Show validation errors in a tooltip.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Server

Handle server-side validation.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/form) documentation for more information.

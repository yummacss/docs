export const meta = {
  title: "Logo Section",
  description: "Showcases partner, client, or brand logos in a grid or row layout.",
};

## Examples

### Basic

<Preview id="logo-section-basic" variant="noPadding" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Heading

<Preview id="logo-section-heading" variant="noPadding" />

```tsx
// wip: reuse registry content to avoid mismatches
```

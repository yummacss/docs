export const meta = {
  title: "Checkbox",
  description: "An easily stylable checkbox component.",
};

## Examples

### Basic

A basic Checkbox implementation.

<Preview id="checkbox-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Indicator

Customize the appearance of the checkbox indicator.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Indeterminate

Show a checkbox in an indeterminate state.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/checkbox) documentation for more information.


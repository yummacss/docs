export const meta = {
  title: "Checkbox",
  description: "A toggle for binary choices.",
};

## React

### Basic

<Preview id="checkbox-basic" />

```tsx
import { Checkbox } from "@base-ui/react/checkbox";
import { CheckIcon } from "@phosphor-icons/react";

export default function CheckboxBasic() {
  return (
    <label className="d-f ai-c g-2 fs-sm c-slate">
      <Checkbox.Root defaultChecked className="d-f ai-c jc-c d-5 br-0 bw-1 bc-silver-4 bg-white">
        <Checkbox.Indicator className="d-f c-black">
          <CheckIcon size={12} weight="bold" />
        </Checkbox.Indicator>
      </Checkbox.Root>
      Enable notifications
    </label>
  );
}
```

## HTML

### Basic Example

```html
<label class="d-f ai-c g-2 fs-sm c-slate">
  <input type="checkbox" class="d-4 ac-slate" />
  Enable notifications
</label>
```

## Documentation

For more information, check the [Base UI Checkbox documentation](https://base-ui.com/react/components/checkbox).

## API Reference

For detailed API reference, see the [Base UI Checkbox API Reference](https://base-ui.com/react/components/checkbox#api-reference).

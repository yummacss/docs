import { templates } from "@/utils/templates";
import Image from "next/image";
import Link from "next/link";

export const meta = {
  title: "Templates",
  description: "A growing collection of Next.js templates. Each template is fully customizable and built with modern best practices.",
};

<div className="d-f fd-c g-16 my-8">
  {templates.map((template) => {
    const isAvailable = template.status === "available";
    
    return (
      <div key={template.slug} className="d-f fd-c lg:fd-r g-8">
        {/* Left - Info */}
        <div className="f-s-0 w-full max-w-70">
          {isAvailable ? (
            <Link href={`/ui/templates/${template.slug}`} className="fs-lg fw-600 c-white mb-1 d-b">
              {template.name}
            </Link>
          ) : (
            <span className="fs-lg fw-600 c-white/50 mb-1 d-b">{template.name}</span>
          )}
          <p className="fs-sm c-white/50 m-0 mb-3">{template.tagline}</p>
          <p className="fs-sm c-white/60 m-0 mb-4 lh-4">{template.description}</p>
          <p className="fs-sm c-white/50 m-0">
            {isAvailable ? (
              <>
                <span className="c-white fw-500">€{template.price}</span>
                {" / included in "}
                <Link href="/ui/license" className="c-white h:td-u">Unlimited Projects</Link>
              </>
            ) : (
              <span className="c-white/40">Coming soon</span>
            )}
          </p>
        </div>

        {/* Right - Screenshot */}
        <div className="f-1">
          {isAvailable ? (
            <Link
              href={`/ui/templates/${template.slug}`}
              className="p-r ar-16/9 o-h br-0 bg-white/5 d-b h:o-90 tr-a"
            >
              <Image
                src={template.cover}
                alt={`${template.name} preview`}
                fill
                className="of-c"
                unoptimized
              />
            </Link>
          ) : (
            <div className="p-r ar-16/9 o-h br-0 bg-white/5 d-f ai-c jc-c">
              <span className="fs-sm c-white/30">Preview coming soon</span>
            </div>
          )}
        </div>
      </div>
    );
  })}
</div>

export const meta = {
  title: "Footer",
  description: "Provides site-wide links at the bottom of pages.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This footer component is made with Base UI and styled with Yumma CSS.

<Preview id="footer-basic" variant="noPadding" />

```tsx registry="footer-basic"
```

### Columns

<Preview id="footer-columns" variant="noPadding" />

```tsx registry="footer-columns"
```

### Logo

<Preview id="footer-columns-with-logo" variant="noPadding" />

```tsx registry="footer-columns-with-logo"
```

export const meta = {
  title: "Collapsible",
  description: "Show & hide content with smooth transitions.",
};

## Installation

```bash
pnpm add @base-ui/react motion
```

## Examples

Implement this component in your project using Yumma CSS and Base UI.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-100 bw-0" registryId="collapsible-base" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="collapsible-base" ```
  </TabsPanel>
</Tabs>

---

### Bordered

Trigger and panel are contained inside a single card, separated by an internal divider when open.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-100 bw-0" registryId="collapsible-bordered" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="collapsible-bordered" ```
  </TabsPanel>
</Tabs>

---

### Icons

A rich trigger with an icon, title, and subtitle paired with a structured key-value panel.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-100 bw-0" registryId="collapsible-icon" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="collapsible-icon" ```
  </TabsPanel>
</Tabs>

---

### Static

Open and close instantly, without animation.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-100 bw-0" registryId="collapsible-static" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="collapsible-static" ```
  </TabsPanel>
</Tabs>

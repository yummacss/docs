export const meta = {
  title: "Collapsible",
  description: "Show & hide content with smooth transitions.",
};

## Installation

```bash
pnpm add @base-ui/react motion
```

## Examples

Implement this component in your project using Yumma CSS and Base UI.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0 h-100" id="collapsible-base" />
  </TabsPanel>
  <TabsPanel value="code">```tsx registry="collapsible-base" ```</TabsPanel>
</Tabs>

---

### Bordered

Trigger and panel are contained inside a single card, separated by an internal divider when open.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0 h-100" id="collapsible-bordered" />
  </TabsPanel>
  <TabsPanel value="code">```tsx registry="collapsible-bordered" ```</TabsPanel>
</Tabs>

---

### Static

No animation. A rich trigger with an icon, title, and subtitle paired with a structured key-value panel.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0 h-100" id="collapsible-static" />
  </TabsPanel>
  <TabsPanel value="code">```tsx registry="collapsible-static" ```</TabsPanel>
</Tabs>

export const meta = {
  title: "Timeline",
  description: "Displays a list of events in chronological order.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This alert dialog component is made with Base UI and styled with Yumma CSS.

<Preview id="timeline-basic" />

```tsx registry="timeline-basic"
```

### With icons

<Preview id="timeline-icons" />

```tsx registry="timeline-icons"
```

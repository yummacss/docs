export const meta = {
  title: "Timeline",
  description: "Displays a list of events in chronological order.",
};

## Examples

### Basic

A basic Timeline implementation.

<Preview id="timeline-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Icons

A Timeline with icons.

<Preview id="timeline-icons" />

```tsx
// wip: reuse registry content to avoid mismatches
```

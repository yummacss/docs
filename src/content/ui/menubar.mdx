export const meta = {
  title: "Menubar",
  description: "A horizontal menu bar with multiple dropdown menus.",
};

## Examples

### Basic

A basic Menubar implementation.

<Preview id="menubar-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Submenu

Add nested submenus to the menubar.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Controls

Add checkboxes and radios to the menubar.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/menubar) documentation for more information.

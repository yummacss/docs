export const meta = {
  title: "Popover",
  description: "A small overlay that appears next to a trigger element.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Examples

This example shows how to use Yumma CSS and Base UI to create an popover component.

<Preview id="popover-basic" />

```tsx registry="popover-basic"
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/popover) documentation for more information.

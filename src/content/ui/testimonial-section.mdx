export const meta = {
  title: "Testimonial Section",
  description: "Displays customer reviews, testimonials, and social proof to build trust.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This example shows how to use Yumma CSS and Base UI to create an alert dialog block.

<Preview id="testimonial-section-basic" />

```tsx registry="testimonial-section-basic"
```

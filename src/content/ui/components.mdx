export const meta = {
  title: "Components",
  description:
    "A collection of UI components styled with Yumma CSS and Base UI for accessibility, keyboard navigation, and focus management.",
};

import { uiSidebarConfig } from "@/utils/ui-sidebar";
import Link from "next/link";

## Base Components

There are {uiSidebarConfig[1].items.filter((item) => !item.wip).length} base components available.

<div className="d-g gtc-1 rg-8 my-10 sm:gtc-3">
  {uiSidebarConfig[1].items.map((item) => (
    <div key={item.slug}>
      <Link className="c-white h:td-u" href={`/ui/${item.slug}`}>
        {item.title}
      </Link>
    </div>
  ))}
</div>

---

## UI Blocks

There are no UI blocks available yet, but it's on the roadmap!
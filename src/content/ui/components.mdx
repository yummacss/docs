import { uiSidebarConfig } from "@/utils/ui-sidebar";
import Link from "next/link";

export const meta = {
   title: "Introduction",
   description: "A set of simple UI components styled with Yumma CSS and Base UI for accessibility, keyboard navigation, and focus management.",
};

## Components

There are {uiSidebarConfig[1].items.length} components available

<div className="d-g gtc-1 sm:gtc-3 rg-8 my-10">
  {uiSidebarConfig[1].items.map(item => (
    <div key={item.slug}>
      <Link className="h:td-u c-white" href={`/ui/${item.slug}`}>{item.title}</Link>
    </div>
  ))}
</div>

---

## Blocks

There are {uiSidebarConfig[2].items.length} blocks available

<div className="d-g gtc-1 sm:gtc-3 rg-8 my-10">
  {uiSidebarConfig[2].items.map(item => (
    <div key={item.slug}>
      <Link className="h:td-u c-white" href={`/ui/${item.slug}`}>{item.title}</Link>
    </div>
  ))}
</div>

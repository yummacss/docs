export const meta = {
  title: "Contact Section",
  description: "Provides contact forms, information, and methods for users to reach out.",
};

## Examples

### Basic

A basic Contact Section implementation.

<Preview id="contact-section-basic" variant="noPadding" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Form

A Contact Section with a form.

<Preview id="contact-section-form" variant="noPadding" />

```tsx
// wip: reuse registry content to avoid mismatches
```

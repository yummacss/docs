export const meta = {
  title: "Contact Section",
  description: "Provides contact forms, information, and methods for users to reach out.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This example shows how to use Yumma CSS and Base UI to create an contact section block.

<Preview id="contact-section-basic" variant="noPadding" />

```tsx registry="contact-section-basic"
```

### Form

<Preview id="contact-section-form" variant="noPadding" />

```tsx registry="contact-section-form"
```

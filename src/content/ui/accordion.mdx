export const meta = {
  title: "Accordion",
  description: "A set of collapsible panels with headings.",
};

## Examples

### Basic

A basic Accordion implementation.

<Preview id="accordion-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Multiple

Use the `multiple` prop to open multiple panels at the same time.

<Preview id="accordion-multiple" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/accordion) documentation for more information.
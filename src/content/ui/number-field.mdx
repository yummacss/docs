export const meta = {
  title: "Number Field",
  description: "Capture numeric input with controls.",
};

## Installation

```bash
pnpm add @base-ui/react
```

## Examples

Free, accessible React number field components built with Base UI for quantity inputs, stepper controls, and numeric forms with increment/decrement buttons.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="number-field-base" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="number-field-base"
    ```
  </TabsPanel>
</Tabs>

export const meta = {
  title: "Tooltip",
  description: "A popup that appears when an element is hovered or focused.",
};

## Examples

### Basic

A basic Tooltip implementation.

<Preview id="tooltip-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Positioning

Customize the positioning of the tooltip.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Delay

Customize the open/close delay of the tooltip.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/tooltip) documentation for more information.

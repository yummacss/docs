export const meta = {
  title: "Pricing Section",
  description: "Presents pricing plans, tiers, and package options for products or services.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This example shows how to use Yumma CSS and Base UI to create an pricing section block.

<Preview id="pricing-section-basic" variant="noPadding" />

```tsx registry="pricing-section-basic"
```

import TemplateFAQ from "@/components/ui/templates/faq";
import TemplateFeatures from "@/components/ui/templates/features";
import TemplateStack from "@/components/ui/templates/stack";
import { getTemplateBySlug, templateFeatures } from "@/utils/templates";
import Image from "next/image";
import { SiNextdotjs, SiReact } from "react-icons/si";

export const meta = {
  title: "Neutra",
  description: "This template is designed for agencies and studios, and it showcases a minimal grayscale theme.",
};

export const template = getTemplateBySlug("neutra");

export const hasNextjs = template.stack.some(item => item.toLowerCase().includes("next.js"));
export const hasReact = template.stack.some(item => item.toLowerCase().includes("react"));

<div className="d-f g-4 mb-8">
  {hasNextjs && (
    <div className="d-if ai-c g-2 fs-sm c-white/70">
      <SiNextdotjs size={20} />
      <span>{template.stack.find(item => item.toLowerCase().includes("next.js"))}</span>
    </div>
  )}
  {hasReact && (
    <div className="d-if ai-c g-2 fs-sm c-white/70">
      <SiReact size={20} />
      <span>{template.stack.find(item => item.toLowerCase().includes("react"))}</span>
    </div>
  )}
</div>

---

## Screenshots

Preview of the template pages.

<div className="d-f fd-c g-4 my-8">
  {template.screenshots.map((screenshot, index) => (
    <div
      key={screenshot}
      className="p-r ar-16/9 o-h bw-1 bs-d bc-white/10 br-0 bg-black/10"
    >
      <Image src={screenshot} alt={`Screenshot ${index + 1}`} fill className="of-c us-none" unoptimized />
    </div>
  ))}
</div>

---

## Features

Everything that makes this template special.

<div className="my-8">
  <TemplateFeatures features={templateFeatures} />
</div>

---

## Built with

Here are the technologies used to build {template.name}.

<div className="my-8">
  <TemplateStack stack={template.stack} />
</div>

---

## Frequently Asked Questions

<TemplateFAQ />

import { getTemplateBySlug } from "@/utils/templates";
import { CheckIcon } from "@phosphor-icons/react/dist/ssr";
import { YummaCSS } from "@react-symbols/icons";
import Image from "next/image";
import { SiNextdotjs, SiPnpm, SiReact, SiTypescript } from "react-icons/si";

export const meta = {
  title: "Neutra",
  description: "A clean, minimal landing page template with a navbar, rating section, call-to-action, and logo section. Perfect for SaaS products, apps, or any modern web project.",
};

export const template = getTemplateBySlug("neutra");

<div className="d-f g-4 mb-8">
  {template.frameworks.map((fw) => (
    <div key={fw.name} className="d-if ai-c g-2 fs-sm c-white/70">
      {fw.name === "Next.js" && <SiNextdotjs size={18} />}
      {fw.name === "React" && <SiReact size={18} />}
      <span>{fw.name} {fw.version}</span>
    </div>
  ))}
</div>

---

## Screenshots

Preview of the template pages.

<div className="d-f fd-c g-4 my-8">
  {template.screenshots.map((screenshot, index) => (
    <div
      key={screenshot}
      className="p-r ar-16/9 o-h bw-1 bs-d bc-white/10 br-0 bg-black/10"
    >
      <Image src={screenshot} alt={`Screenshot ${index + 1}`} fill className="of-c" />
    </div>
  ))}
</div>

---

## Pages

This template includes {template.pageCount} pages.

<div className="d-g gtc-2 md:gtc-4 g-4 my-8">
  {template.pages.map((page) => (
    <div
      key={page.path}
      className="p-4 bw-1 bc-white/10 bs-d br-0 d-f ai-c jc-c bg-black/10"
    >
      <span className="fs-sm c-white">{page.title}</span>
    </div>
  ))}
</div>

---

## What's included

Everything you need to get started.

<div className="d-g gtc-1 md:gtc-2 g-4 my-8">
  <div className="d-f ai-c g-3">
    <CheckIcon size={16} className="c-white" weight="bold" />
    <span className="fs-sm c-white/70">Full source code</span>
  </div>
  <div className="d-f ai-c g-3">
    <CheckIcon size={16} className="c-white" weight="bold" />
    <span className="fs-sm c-white/70">Neutral & colorful versions</span>
  </div>
  <div className="d-f ai-c g-3">
    <CheckIcon size={16} className="c-white" weight="bold" />
    <span className="fs-sm c-white/70">Lifetime updates</span>
  </div>
  <div className="d-f ai-c g-3">
    <CheckIcon size={16} className="c-white" weight="bold" />
    <span className="fs-sm c-white/70">Discord support</span>
  </div>
</div>

---

## Built with

Here are the technologies used to build {template.name}.

<div className="d-g gtc-2 md:gtc-3 g-4 my-8">
  <div
    className="p-4 bw-1 bc-white/10 bs-d br-0 d-f ai-c jc-c g-2 bg-black/10"
  >
    <SiNextdotjs size={20} fill="white" />
    <span className="fs-sm c-white">Next.js 16</span>
  </div>
  <div
    className="p-4 bw-1 bc-white/10 bs-d br-0 d-f ai-c jc-c g-2 bg-black/10"
  >
    <SiReact size={20} fill="white" />
    <span className="fs-sm c-white">React 19</span>
  </div>
  <div
    className="p-4 bw-1 bc-white/10 bs-d br-0 d-f ai-c jc-c g-2 bg-black/10"
  >
    <YummaCSS width={20} height={20} />
    <span className="fs-sm c-white">Yumma CSS 3</span>
  </div>
  {/* pnpm */}
  <div className="p-4 bw-1 bc-white/10 bs-d br-0 d-f ai-c jc-c g-2 bg-black/10">
    <SiPnpm size={20} fill="white" />
    <span className="fs-sm c-white">pnpm 10</span>
  </div>
  <div
    className="p-4 bw-1 bc-white/10 bs-d br-0 d-f ai-c jc-c g-2 bg-black/10"
  >
    <SiTypescript size={20} fill="white" />
    <span className="fs-sm c-white">TypeScript 5</span>
  </div>
</div>

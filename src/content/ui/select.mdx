export const meta = {
  title: "Select",
  description: "Choose from a dropdown list of options.",
};

## Installation

```bash
pnpm add @base-ui/react motion
```

## Examples

Free, accessible React select components built with Base UI for dropdown options, country pickers, and form selects with search filtering.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0 h-sm" registryId="select-base" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="select-base"
    ```
  </TabsPanel>
</Tabs>
---

### Grouped

Group options by category using the `Select.Group` component.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0 h-sm" registryId="select-grouped" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="select-grouped"
    ```
  </TabsPanel>
</Tabs>

---

### Static

No animation utilities or animation dependencies.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0 h-sm" registryId="select-static" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="select-static"
    ```
  </TabsPanel>
</Tabs>

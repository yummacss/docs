export const meta = {
  title: "Select",
  description: "A common form component for choosing a predefined value in a dropdown menu.",
};

## Examples

### Basic

A basic Select implementation.

<Preview id="select-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Multiple

Allow users to select multiple options.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Groups

Display options in groups.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Value

Customize the appearance of the selected value.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/select) documentation for more information.


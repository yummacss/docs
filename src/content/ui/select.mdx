export const meta = {
  title: "Select",
  description: "A common form component for choosing a predefined value in a dropdown menu.",
};

## React

An interactive component powered by Base UI and styled with Yumma CSS.

### Basic

<Preview id="select-basic" />

```tsx
"use client";

import { Select } from "@base-ui/react/select";
import { CaretUpDownIcon, CheckIcon } from "@phosphor-icons/react";

export default function ExampleSelect() {
  return (
    <Select.Root>
      <Select.Trigger className="d-f h-10 min-w-36 ai-c jc-sb g-3 br-1 px-3 bw-1 bc-silver-4 bg-white c-slate us-none h:bg-silver-1 fv:os-s fv:ow-2 fv:oo--1 fv:oc-blue-8">
        <Select.Value placeholder="Select font..." />
        <Select.Icon className="d-f">
          <CaretUpDownIcon size={10} />
        </Select.Icon>
      </Select.Trigger>
      <Select.Portal>
        <Select.Positioner sideOffset={8} className="ow-0 us-none zi-10">
          <Select.Popup className="br-1 bg-white c-slate bs-md bw-1 bc-silver-4">
            <Select.List className="p-r py-1 o-a">
              {fonts.map(({ label, value }) => (
                <Select.Item
                  key={label}
                  value={value}
                  className="d-g ai-c g-2 py-2 px-3 ow-0 us-none c-d"
                >
                  <Select.ItemIndicator className="gcs-1">
                    <CheckIcon size={12} />
                  </Select.ItemIndicator>
                  <Select.ItemText className="gcs-2">{label}</Select.ItemText>
                </Select.Item>
              ))}
            </Select.List>
          </Select.Popup>
        </Select.Positioner>
      </Select.Portal>
    </Select.Root>
  );
}

const fonts = [
  { label: "Sans-serif", value: "sans" },
  { label: "Serif", value: "serif" },
  { label: "Monospace", value: "mono" },
  { label: "Cursive", value: "cursive" },
];
```

## HTML

A vanilla HTML and CSS implementation.

### Basic Example

```html
<div class="p-r">
  <button class="d-f h-10 min-w-36 ai-c jc-sb g-3 br-1 px-3 bw-1 bc-silver-4 bg-white c-slate us-none h:bg-silver-1">
    <span>Select font...</span>
    ▼
  </button>
  <!-- Dropdown (shown as open) -->
  <div class="p-a t-full l-0 mt-2 br-1 bg-white c-slate bs-md bw-1 bc-silver-4 min-w-36">
    <div class="d-g ai-c g-2 py-2 px-3 h:bg-silver-1 fs-sm">Sans-serif</div>
    <div class="d-g ai-c g-2 py-2 px-3 h:bg-silver-1 fs-sm">Serif</div>
    <div class="d-g ai-c g-2 py-2 px-3 h:bg-silver-1 fs-sm">Monospace</div>
    <div class="d-g ai-c g-2 py-2 px-3 h:bg-silver-1 fs-sm">Cursive</div>
  </div>
</div>
```


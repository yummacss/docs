export const meta = {
  title: "Auth Form",
  description: "Authentication forms for login and registration.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Examples

This example shows how to use Yumma CSS and Base UI to create an auth form block.

### Sign In

<Preview id="auth-form-basic-sign-in" />

```tsx registry="auth-form-basic-sign-in"
```

### Sign Up

<Preview id="auth-form-basic-sign-up" />

```tsx registry="auth-form-basic-sign-up"
```
export const meta = {
  title: "Menu",
  description: "A list of actions in a dropdown, enhanced with keyboard navigation.",
};

## Examples

### Basic

A basic Menu implementation.

<Preview id="menu-basic" variant="inlineCentered" className="bg-white" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Submenu

Add nested submenus to the menu.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Controls

Add checkboxes and radios to the menu.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/menu) documentation for more information.

export const meta = {
  title: "Statistics Section",
  description: "Showcases key metrics, numbers, and data points in a visually prominent way.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Examples

### Basic

This example shows how to use Yumma CSS and Base UI to create an alert dialog block.

<Preview id="statistics-section-basic" variant="noPadding" />

```tsx registry="statistics-section-basic"
```

### With icons

<Preview id="statistics-section-icons" variant="noPadding" />

```tsx registry="statistics-section-icons"
```

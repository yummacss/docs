export const meta = {
  title: "Dialog",
  description: "Display modal content over your application.",
};

## Installation

```bash
pnpm add @base-ui/react motion
```

## Core Examples

Free, accessible React dialog components built with Base UI for confirmations, forms, and modals with focus trapping and smooth animations.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="dialog-base" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="dialog-base"
    ```
  </TabsPanel>
</Tabs>

---

### Confirmation

A dialog that requires destructive action before performing a destructive action.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="alert-dialog-confirmation" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="alert-dialog-confirmation"
    ```
  </TabsPanel>
</Tabs>

---

### Scrollable

A dialog with scrollable content when overflowing the viewport.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="dialog-scrollable" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="dialog-scrollable"
    ```
  </TabsPanel>
</Tabs>

---

### Form

A dialog that contains a form for user input with validation.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="dialog-form" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="dialog-form"
    ```
  </TabsPanel>
</Tabs>

---

### Static

No animation utilities or animation dependencies.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="dialog-static" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="dialog-static"
    ```
  </TabsPanel>
</Tabs>

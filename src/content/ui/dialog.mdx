export const meta = {
  title: "Dialog",
  description: "A popup that opens on top of the entire page.",
};

## Examples

### Basic

<Preview id="dialog-basic" variant="inlineCentered" className="bg-white" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Form

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Nested

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/dialog) documentation for more information.


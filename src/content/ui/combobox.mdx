export const meta = {
  title: "Combobox",
  description: "An input combined with a list of predefined items to select.",
};

## Examples

### Basic

A basic Combobox implementation.

<Preview id="combobox-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Grouped

Display options in groups.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Item

Customize the appearance of individual items.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Validation

Add form validation to the combobox.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/combobox) documentation for more information.


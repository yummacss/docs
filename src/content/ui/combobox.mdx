export const meta = {
  title: "Combobox",
  description: "Combine text input with dropdown selection.",
};

## Installation

```bash
pnpm add @base-ui/react motion
```

## Examples

Free, accessible React combobox components built with Base UI for searchable dropdowns, multi-select tags, and filtered suggestions with keyboard support.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="combobox-base" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="combobox-base"
    ```
  </TabsPanel>
</Tabs>

---

### Multiple

Enable multi-select with the `multiple` prop. Selected items render as removable chip tags inside the input.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="combobox-multiple" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="combobox-multiple"
    ```
  </TabsPanel>
</Tabs>

---

### Inline

Use the `inline` prop to render the list directly in the page without a floating popup.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="combobox-inline" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="combobox-inline"
    ```
  </TabsPanel>
</Tabs>

---

### Grouped

Group items by category using the `group` prop for organized dropdown options.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="combobox-grouped" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="combobox-grouped"
    ```
  </TabsPanel>
</Tabs>

---

### Loading

Display a loading skeleton while fetching filtered results from the server.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="combobox-loading" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="combobox-loading"
    ```
  </TabsPanel>
</Tabs>

---

### Static

No animation utilities or animation dependencies.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="bw-0" registryId="combobox-static" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="combobox-static"
    ```
  </TabsPanel>
</Tabs>

export const meta = {
  title: "Autocomplete",
  description: "An input that suggests options as you type.",
};

## Examples

### Basic

<Preview id="autocomplete-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Async search

```tsx
// wip: reuse registry content to avoid mismatches
```

### Inline autocomplete

```tsx
// wip: reuse registry content to avoid mismatches
```

### Grouped

```tsx
// wip: reuse registry content to avoid mismatches
```

### Fuzzy matching

```tsx
// wip: reuse registry content to avoid mismatches
```

### Limit results

```tsx
// wip: reuse registry content to avoid mismatches
```

### Auto highlight

```tsx
// wip: reuse registry content to avoid mismatches
```

### Grid layout

```tsx
// wip: reuse registry content to avoid mismatches
```

### Virtualized

```tsx
// wip: reuse registry content to avoid mismatches
```


## API Reference

Check out the [Base UI](https://base-ui.com/react/components/autocomplete) documentation for more information.

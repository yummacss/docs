export const meta = {
  title: "Toast",
  description: "Generates toast notifications.",
};

## Examples

### Basic

A basic Toast implementation.

<Preview id="toast-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Action

A Toast with an action button.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Status

A Toast that indicates status (success, error, etc.).

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Duration

A Toast with custom duration.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/toast) documentation for more information.


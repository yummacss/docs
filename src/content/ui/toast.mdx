export const meta = {
  title: "Toast",
  description: "Display brief notification messages.",
};

## Work in Progress

This component is currently a work in progress and not yet available for use. We are actively developing it and will update this page once it's ready.

## Core Examples

Free Toast accessible React components with Base UI and Yumma CSS.

### Base

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="toast-base" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="toast-base"
    ```
  </TabsPanel>
</Tabs>

---

### Error

Display error messages when something goes wrong using the error variant.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="toast-error" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="toast-error"
    ```
  </TabsPanel>
</Tabs>

---

### Success

Display success messages when an action completes using the success variant.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="toast-success" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="toast-success"
    ```
  </TabsPanel>
</Tabs>

---

### Warning

Display warning messages that require user attention using the warning variant.

<Tabs defaultValue="preview">
  <TabsList>
    <TabsTab value="preview">Preview</TabsTab>
    <TabsTab value="code">Code</TabsTab>
  </TabsList>
  <TabsPanel className="bg-white bbr-sm" value="preview">
    <Preview className="h-56 bw-0" registryId="toast-warning" />
  </TabsPanel>
  <TabsPanel value="code">
    ```tsx registryId="toast-warning"
    ```
  </TabsPanel>
</Tabs>
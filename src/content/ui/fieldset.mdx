export const meta = {
  title: "Fieldset",
  description: "A native fieldset element with an easily stylable legend.",
};

## Examples

### Basic

A basic Fieldset implementation.

<Preview id="fieldset-basic" />

```tsx
// wip: reuse registry content to avoid mismatches
```

### Disabled

Disable the entire fieldset.

<Preview id="wip" />

```tsx
// wip: reuse registry content to avoid mismatches
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/fieldset) documentation for more information.

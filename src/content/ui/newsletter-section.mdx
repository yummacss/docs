export const meta = {
  title: "Newsletter Section",
  description: "Collects email addresses for newsletter subscriptions and mailing list signups.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Examples

This example shows how to use Yumma CSS and Base UI to create an newsletter section block.

<Preview id="newsletter-section-basic" variant="noPadding" />

```tsx registry="newsletter-section-basic"
```

### Centered

<Preview id="newsletter-section-centered-with-icon" variant="noPadding" />

```tsx registry="newsletter-section-centered-with-icon"
```

export const meta = {
  title: "Radio",
  description: "A set of buttons where only one can be selected at a time.",
};

## Installation

Implement this component in your project using Yumma CSS and Base UI.

```bash frame="none"
npm install @base-ui/react
```

## Example

This alert dialog component is made with Base UI and styled with Yumma CSS.

<Preview id="radio-basic" />

```tsx registry="radio-basic"
```

## API Reference

Check out the [Base UI](https://base-ui.com/react/components/radio) documentation for more information.

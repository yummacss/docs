{
  "id": "toggle",
  "component": "toggle",
  "variant": "base",
  "kind": "component",
  "summary": "A two-state icon button, in three shapes, with an optional press animation.",
  "props": [
    {
      "name": "icon",
      "type": "none",
      "typeName": "ReactNode",
      "description": "Shown while not pressed.",
      "exampleIcon": "SunLight"
    },
    {
      "name": "pressedIcon",
      "type": "none",
      "typeName": "ReactNode",
      "description": "Shown while pressed.",
      "exampleIcon": "HalfMoon"
    },
    {
      "name": "defaultPressed",
      "type": "boolean",
      "default": false,
      "description": "Starting state when you are not controlling it. Pair `pressed` with `onPressedChange` instead if you are."
    },
    {
      "name": "pressed",
      "type": "boolean",
      "description": "Controlled state."
    },
    {
      "name": "onPressedChange",
      "type": "none",
      "typeName": "(pressed: boolean) => void",
      "description": "Called with the new state. Required for a controlled toggle."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": [
        "rounded",
        "square",
        "squircle"
      ],
      "default": "rounded",
      "description": "Corner radius. `squircle` uses `corner-shape`, which degrades to a rounded square where that is unsupported."
    },
    {
      "name": "size",
      "type": "enum",
      "values": [
        "sm",
        "md"
      ],
      "default": "md",
      "description": "The button's footprint."
    },
    {
      "name": "tone",
      "type": "enum",
      "values": [
        "accent",
        "ghost"
      ],
      "default": "accent",
      "description": "`accent` is a standalone toggle: bordered and tinted even unpressed. `ghost` is for one that already sits inside a bordered group - plain until pressed."
    },
    {
      "name": "swatchClassName",
      "type": "string",
      "description": "Replaces `tone` entirely rather than adding to it, for a color-picker swatch where the color IS the toggle and stays constant whether pressed or not - `pressedIcon` shows selection there instead."
    },
    {
      "name": "value",
      "type": "string",
      "description": "Identifies this toggle inside a `ToggleGroup`. When set, Base UI derives `pressed` from the group's own value array instead of this component's internal state."
    },
    {
      "name": "animate",
      "type": "boolean",
      "default": true,
      "description": "The press-scale animation. Turn it off for an instant press, or when the user has asked for reduced motion."
    },
    {
      "name": "aria-label",
      "type": "string",
      "example": "Toggle theme",
      "description": "The button has no visible text, so this is the only accessible name it gets. Set it every time."
    },
    {
      "name": "className",
      "type": "string",
      "description": "Appended to the button's classes, for anything the component leaves unset - a margin, `fs-0` in a flex row. It will not reliably override what the component already sets: which of two utilities wins is decided by the stylesheet's rule order, not by the order you write them. Use `size` for the dimensions and `swatchClassName` for the swatch color."
    }
  ],
  "useClient": true,
  "dependencies": [
    {
      "name": "@base-ui/react",
      "version": "^1.7.0"
    },
    {
      "name": "motion",
      "version": "^13.1.0"
    }
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "toggle.tsx",
      "target": "components/ui/toggle.tsx",
      "content": "\"use client\";\n\nimport { Button } from \"@base-ui/react/button\";\nimport { Toggle } from \"@base-ui/react/toggle\";\nimport type { HTMLMotionProps } from \"motion/react\";\nimport { motion } from \"motion/react\";\nimport type { ComponentProps, ReactNode } from \"react\";\nimport { useState } from \"react\";\n\ntype Shape = \"rounded\" | \"square\" | \"squircle\";\ntype Size = \"sm\" | \"md\";\ntype Tone = \"accent\" | \"ghost\";\n\nconst SHAPES: Record<Shape, string> = {\n  rounded: \"br-9999\",\n  square: \"\",\n  squircle: \"br-xxl cs-s\",\n};\n\nconst SIZES: Record<Size, string> = {\n  sm: \"w-9 h-9\",\n  md: \"w-12 h-12\",\n};\n\n// `accent` is a standalone toggle: bordered and tinted even unpressed, since\n// nothing else marks it as interactive. `ghost` is for one that already sits\n// inside a bordered group (a toolbar) - plain until pressed, or every toggle\n// in the row would carry its own redundant border.\nconst TONES: Record<Tone, { pressed: string; unpressed: string }> = {\n  accent: {\n    pressed: \"bg-indigo bc-indigo-6 c-white\",\n    unpressed: \"bg-white bc-indigo-3 c-indigo h:bg-indigo-1\",\n  },\n  ghost: {\n    pressed: \"bg-indigo c-white\",\n    unpressed: \"bg-transparent c-slate-8 h:bg-silver-1 h:c-slate-10\",\n  },\n};\n\nexport interface ToggleProps\n  extends Omit<ComponentProps<\"button\">, \"className\" | \"value\">,\n    Pick<ComponentProps<typeof Toggle>, \"value\"> {\n  /** Shown while not pressed. */\n  icon?: ReactNode;\n  /** Shown while pressed. */\n  pressedIcon?: ReactNode;\n  defaultPressed?: boolean;\n  pressed?: boolean;\n  onPressedChange?: (pressed: boolean) => void;\n  shape?: Shape;\n  size?: Size;\n  tone?: Tone;\n  /**\n   * Replaces `tone` entirely rather than adding to it, for a color-picker\n   * swatch where the color IS the toggle and stays constant whether pressed\n   * or not - `pressedIcon` is what shows selection there, not a color change.\n   */\n  swatchClassName?: string;\n  /** The press-scale animation. Off for an instant toggle, or reduced motion. */\n  animate?: boolean;\n  className?: string;\n}\n\nexport default function ToggleBase({\n  icon,\n  pressedIcon,\n  defaultPressed,\n  pressed: controlledPressed,\n  onPressedChange,\n  shape = \"rounded\",\n  size = \"md\",\n  tone = \"accent\",\n  swatchClassName,\n  animate = true,\n  className,\n  value,\n  ...props\n}: ToggleProps) {\n  // Inside a ToggleGroup, Base UI derives `pressed` from the group's own\n  // value array; passing `pressed` here - even uncontrolled, even `false` -\n  // would override that and break group membership. Standalone (no `value`),\n  // this component owns pressed state itself so the caller does not have to.\n  const grouped = value !== undefined;\n  const [internalPressed, setInternalPressed] = useState(\n    defaultPressed ?? false,\n  );\n  const pressed = controlledPressed ?? internalPressed;\n\n  const handlePressedChange = (next: boolean) => {\n    setInternalPressed(next);\n    onPressedChange?.(next);\n  };\n\n  const pressedProps = grouped\n    ? {}\n    : { pressed, onPressedChange: handlePressedChange, defaultPressed };\n\n  return (\n    <Toggle\n      value={value}\n      {...pressedProps}\n      className={(state) =>\n        [\n          \"d-f ai-c jc-c us-none c-p fv:oo-2 fv:oc-indigo-5\",\n          SIZES[size],\n          SHAPES[shape],\n          swatchClassName\n            ? \"bw-0\"\n            : `bw-1 ${state.pressed ? TONES[tone].pressed : TONES[tone].unpressed}`,\n          swatchClassName,\n          className,\n        ]\n          .filter(Boolean)\n          .join(\" \")\n      }\n      render={(renderProps, state) =>\n        animate ? (\n          <motion.button\n            type=\"button\"\n            {...(renderProps as HTMLMotionProps<\"button\">)}\n            whileTap={{ scale: 0.9 }}\n            transition={{ duration: 0.2, ease: \"easeOut\" }}\n          >\n            {state.pressed ? pressedIcon : icon}\n          </motion.button>\n        ) : (\n          <Button {...(renderProps as ComponentProps<\"button\">)}>\n            {state.pressed ? pressedIcon : icon}\n          </Button>\n        )\n      }\n      {...props}\n    />\n  );\n}\n"
    }
  ]
}

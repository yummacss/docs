{
  "id": "separator",
  "component": "separator",
  "variant": "base",
  "kind": "component",
  "summary": "A horizontal or vertical divider, plain, or with a clickable icon or a text label in the middle.",
  "props": [
    {
      "name": "orientation",
      "type": "enum",
      "values": [
        "horizontal",
        "vertical"
      ],
      "default": "horizontal",
      "description": "A vertical rule for a row layout, like a button group. Ignored alongside `icon` or `label`, which are a horizontal-only pattern."
    },
    {
      "name": "icon",
      "type": "none",
      "typeName": "ReactNode",
      "description": "Renders as a clickable button in the middle, sized & shaped to match. Wins over `label` if both are set."
    },
    {
      "name": "onIconClick",
      "type": "none",
      "typeName": "() => void",
      "description": "Called when the icon button is pressed."
    },
    {
      "name": "label",
      "type": "none",
      "typeName": "ReactNode",
      "example": "or",
      "description": "Renders as plain text in the middle. Ignored if `icon` is set."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": [
        "rounded",
        "square",
        "squircle",
        "circle"
      ],
      "default": "rounded",
      "description": "Corner radius on the icon button. Only visible alongside `icon`."
    },
    {
      "name": "className",
      "type": "string",
      "description": "Appended to the separator's (or wrapper's, when `icon`/`label` is set) classes, for anything the component leaves unset - a margin, `fs-0` in a flex row. It will not reliably override what the component already sets: which of two utilities wins is decided by the stylesheet's rule order, not by the order you write them."
    }
  ],
  "useClient": false,
  "dependencies": [
    {
      "name": "@base-ui/react",
      "version": "^1.7.0"
    }
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "separator.tsx",
      "target": "components/ui/separator.tsx",
      "content": "import { Button } from \"@base-ui/react/button\";\nimport { Separator } from \"@base-ui/react/separator\";\nimport type { ReactNode } from \"react\";\n\ntype Shape = \"rounded\" | \"square\" | \"squircle\" | \"circle\";\n\nconst SHAPES: Record<Shape, string> = {\n  rounded: \"br-lg\",\n  square: \"\",\n  squircle: \"br-xxl cs-s\",\n  circle: \"br-9999\",\n};\n\ntype Orientation = \"horizontal\" | \"vertical\";\n\nexport interface SeparatorProps {\n  /** Renders as a clickable button in the middle, sized & shaped to match. */\n  icon?: ReactNode;\n  onIconClick?: () => void;\n  /** Renders as plain text in the middle. Ignored if `icon` is set. */\n  label?: ReactNode;\n  /** Only visible alongside `icon`. */\n  shape?: Shape;\n  /** A vertical rule for a row layout, like a button group. Ignored alongside `icon` or `label`, which are a horizontal-only pattern. */\n  orientation?: Orientation;\n  className?: string;\n}\n\nexport default function SeparatorBase({\n  icon,\n  onIconClick,\n  label,\n  shape = \"rounded\",\n  orientation = \"horizontal\",\n  className,\n}: SeparatorProps) {\n  if (!icon && !label) {\n    return (\n      <Separator\n        orientation={orientation}\n        className={[\n          orientation === \"vertical\" ? \"w-px h-100%\" : \"h-px w-100%\",\n          \"bg-silver-2\",\n          className,\n        ]\n          .filter(Boolean)\n          .join(\" \")}\n      />\n    );\n  }\n\n  const buttonClasses = [\n    \"d-if ai-c jc-c w-8 h-8 bg-white bc-silver-2 c-slate-10 bw-1 tp-c tdu-150 ttf-io us-none c-p h:bg-silver-1/50 fv:oo-2 fv:oc-indigo-5\",\n    SHAPES[shape],\n  ]\n    .filter(Boolean)\n    .join(\" \");\n\n  return (\n    <div\n      className={[\"d-f ai-c g-2 w-100%\", className].filter(Boolean).join(\" \")}\n    >\n      <Separator className=\"fg-1 h-px bg-silver-2\" />\n      {icon ? (\n        <Button className={buttonClasses} onClick={onIconClick}>\n          {icon}\n        </Button>\n      ) : (\n        <span className=\"c-slate-6 fs-xs fw-500 tt-u ls-3\">{label}</span>\n      )}\n      <Separator className=\"fg-1 h-px bg-silver-2\" />\n    </div>\n  );\n}\n"
    }
  ]
}
